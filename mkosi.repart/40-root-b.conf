[Partition]
Type=root
Label=_empty
ReadOnly=yes
SizeMinBytes=512M
# Empty spare slot: do not emit a split artifact (only the populated A slot is the
# update artifact); otherwise both root partitions resolve to the same split name.
SplitName=-
