[Partition]
Type=var
Format=ext4
Label=var
SizeMinBytes=256M
GrowFileSystem=yes
# Generate a baked /etc/fstab entry so /var mounts deterministically (writable),
# without depending on GPT auto-discovery / machine-id-derived UUIDs.
MountPoint=/var
