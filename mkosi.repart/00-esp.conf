[Partition]
Type=esp
Format=vfat
# Populate the ESP with the bootloader and the UKI; without CopyFiles the custom
# repart layout leaves the ESP empty and the firmware finds nothing to boot.
CopyFiles=/efi:/
CopyFiles=/boot:/
SizeMinBytes=512M
SizeMaxBytes=512M
