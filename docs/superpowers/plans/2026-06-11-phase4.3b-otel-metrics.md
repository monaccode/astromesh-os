# Fase 4.3b — Métricas OTel (derivadas del TracingContext) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Exportar el set completo de métricas del engine (runs, latencia, tokens, costo, llamadas a provider, ejecuciones de tool) vía OTLP al collector sidecar, derivándolas del `TracingContext` que el runtime ya produce por corrida.

**Architecture:** Cambio de fuente quirúrgico en el runtime (`../astromesh`): extender el `MetricsManager` de 4.4c (un MeterProvider/OTLP ya existente) de 1 counter al set completo, y un método `record_run(ctx)` que recorre los spans y alimenta los instrumentos, cableado en el único punto de emit (`engine.run()` `finally`, junto a `emit_trace`/`flush`). En el repo de OS (`astromesh-os`): solo un gate nuevo + target de dev-loop + CI; cero cambios de infraestructura (el collector ya tiene pipeline `metrics`, el binario `otelcol` ya se hornea, la guarda off-by-default ya existe).

**Tech Stack:** Python (opentelemetry-sdk metrics, OTLP/gRPC exporter), pytest/pytest-asyncio (runtime), bash + QEMU/KVM + mkosi (gate de OS), GitHub Actions.

**Dos repos:**
- **Node:** `/Users/fulfaro/monaccode/astromesh` (Tareas 1–2; commits en ese repo).
- **OS:** `/Users/fulfaro/monaccode/astromesh-os` (Tareas 3–6; commits en este repo).

**Spec:** `docs/superpowers/specs/2026-06-11-phase4.3b-otel-metrics-design.md`.

---

## File Structure

- **`/Users/fulfaro/monaccode/astromesh/astromesh/observability/metrics_export.py`** (mod) — extiende `MetricsManager.__init__`/`setup()` con el set de instrumentos y agrega `record_run(ctx)`. Misma responsabilidad (métricas app-side → OTLP), un solo MeterProvider/flush.
- **`/Users/fulfaro/monaccode/astromesh/tests/test_engine_metrics.py`** (new) — tests del nodo: `record_run` deriva los valores correctos; el engine llama `record_run` en cada corrida.
- **`/Users/fulfaro/monaccode/astromesh/astromesh/runtime/engine.py`** (mod, `finally` ~648) — una línea: `_m2.record_run(tracing)` antes de `_m2.flush()`.
- **`tests/boot/otel-metrics-and-assert.sh`** (new, OS) — el gate.
- **`tests/local/dev-loop.sh`** (mod, OS) — target `otel-metrics`.
- **`.github/workflows/phase4-otel-metrics.yml`** (new, OS) — CI.
- **`docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`** (mod, OS) — fila de roadmap 4.3 actualizada.

---

## Task 1: Set completo de instrumentos + `record_run` en el `MetricsManager` (node)

**Files:**
- Modify: `/Users/fulfaro/monaccode/astromesh/astromesh/observability/metrics_export.py`
- Test: `/Users/fulfaro/monaccode/astromesh/tests/test_engine_metrics.py` (create)

- [ ] **Step 1: Escribir el test que falla (derivación de un TracingContext sintético)**

Crear `/Users/fulfaro/monaccode/astromesh/tests/test_engine_metrics.py`:

```python
"""Fase 4.3b: engine metrics derived from the TracingContext span tree."""


class _FakeInstr:
    def __init__(self, name, sink):
        self.name = name
        self._sink = sink

    def add(self, value, attrs=None):
        self._sink.append((self.name, "add", value, attrs or {}))

    def record(self, value, attrs=None):
        self._sink.append((self.name, "record", value, attrs or {}))


def _mgr_with_fakes(sink):
    from astromesh.observability import metrics_export as mx
    m = mx.MetricsManager(enabled=False)  # disabled -> setup() creates no real instruments
    m.setup()
    # inject capturing fakes (matches the private-attr style of test_agent_egress_metric.py)
    m._runs = _FakeInstr("runs", sink)
    m._latency = _FakeInstr("latency", sink)
    m._llm_calls = _FakeInstr("llm_calls", sink)
    m._llm_latency = _FakeInstr("llm_latency", sink)
    m._tokens = _FakeInstr("tokens", sink)
    m._cost = _FakeInstr("cost", sink)
    m._tools = _FakeInstr("tools", sink)
    return m


def _build_ctx():
    from astromesh.observability.tracing import TracingContext, SpanStatus
    ctx = TracingContext(agent_name="rec-agent", session_id="s1")
    root = ctx.start_span("agent.run", {"agent": "rec-agent", "session": "s1"})
    llm = ctx.start_span("llm.complete")
    llm.set_attribute("model", "m"); llm.set_attribute("provider", "p")
    llm.set_attribute("latency_ms", 1500.0); llm.set_attribute("cost", 0.02)
    llm.set_attribute("input_tokens", 10); llm.set_attribute("output_tokens", 5)
    ctx.finish_span(llm, status=SpanStatus.OK)
    tool = ctx.start_span("tool.call", {"tool": "search"})
    ctx.finish_span(tool, status=SpanStatus.OK)
    ctx.finish_span(root, status=SpanStatus.OK)
    return ctx


def test_record_run_derives_full_metric_set():
    sink = []
    m = _mgr_with_fakes(sink)
    m.record_run(_build_ctx())

    # agent.run -> runs counter + latency histogram
    assert ("runs", "add", 1, {"agent": "rec-agent", "status": "ok"}) in sink
    assert any(n == "latency" and op == "record" and v > 0 and a == {"agent": "rec-agent"}
               for (n, op, v, a) in sink)
    # llm.complete -> llm.calls + llm.latency + tokens(in/out) + cost
    assert ("llm_calls", "add", 1, {"provider": "p", "model": "m", "status": "ok"}) in sink
    assert any(n == "llm_latency" and abs(v - 1.5) < 1e-6 for (n, op, v, a) in sink)
    assert ("tokens", "add", 10, {"agent": "rec-agent", "direction": "input"}) in sink
    assert ("tokens", "add", 5, {"agent": "rec-agent", "direction": "output"}) in sink
    assert ("cost", "add", 0.02, {"agent": "rec-agent", "model": "m"}) in sink
    # tool.call -> tool.executions
    assert ("tools", "add", 1, {"tool": "search", "status": "ok"}) in sink


def test_record_run_is_best_effort_when_disabled():
    from astromesh.observability import metrics_export as mx
    m = mx.MetricsManager(enabled=False)
    m.setup()  # instruments stay None
    # must not raise even with no instruments and an empty/None ctx
    m.record_run(_build_ctx())

    class _Empty:
        spans = []
        agent_name = "x"
    m.record_run(_Empty())
```

- [ ] **Step 2: Correr el test para verque falla**

Run: `cd /Users/fulfaro/monaccode/astromesh && python3 -m pytest tests/test_engine_metrics.py -v`
Expected: FAIL — `AttributeError: 'MetricsManager' object has no attribute 'record_run'` (y `_runs`, etc. no existen aún).

- [ ] **Step 3: Implementar el set de instrumentos + `record_run`**

En `/Users/fulfaro/monaccode/astromesh/astromesh/observability/metrics_export.py`:

(a) En `__init__`, reemplazar `self._counter = None` por la inicialización de todos los instrumentos a `None`:

```python
    def __init__(self, endpoint: str = "http://127.0.0.1:4317", enabled: bool = False):
        self._endpoint = endpoint
        self._enabled = enabled
        self._provider = None
        self._counter = None          # 4.4c: astromesh.agent.egress.bytes
        self._runs = None             # 4.3b: engine-derived instruments
        self._latency = None
        self._llm_calls = None
        self._llm_latency = None
        self._tokens = None
        self._cost = None
        self._tools = None
```

(b) En `setup()`, después de `self._counter = meter.create_counter("astromesh.agent.egress.bytes", unit="By")`, crear el resto (mismo `meter`):

```python
            self._counter = meter.create_counter("astromesh.agent.egress.bytes", unit="By")
            # 4.3b: full engine metric set, derived per-run from the TracingContext.
            self._runs = meter.create_counter("astromesh.agent.runs", unit="1")
            self._latency = meter.create_histogram("astromesh.agent.latency", unit="s")
            self._llm_calls = meter.create_counter("astromesh.llm.calls", unit="1")
            self._llm_latency = meter.create_histogram("astromesh.llm.latency", unit="s")
            self._tokens = meter.create_counter("astromesh.agent.tokens", unit="1")
            self._cost = meter.create_counter("astromesh.agent.cost", unit="USD")
            self._tools = meter.create_counter("astromesh.tool.executions", unit="1")
```

(c) En el bloque `except` de `setup()`, junto a `self._counter = None`, anular también los nuevos:

```python
        except Exception:
            import logging
            logging.getLogger("astromeshd").warning("agent metrics unavailable", exc_info=True)
            self._counter = None
            self._runs = self._latency = self._llm_calls = self._llm_latency = None
            self._tokens = self._cost = self._tools = None
```

(d) Agregar el método `record_run` (después de `record`, antes de `flush`):

```python
    def record_run(self, ctx) -> None:
        """4.3b: derive the engine metric set from a completed TracingContext. Best-effort; never raises."""
        try:
            spans = getattr(ctx, "spans", None) or []
            agent_name = getattr(ctx, "agent_name", "unknown")
            for s in spans:
                name = getattr(s, "name", "")
                attrs = getattr(s, "attributes", {}) or {}
                status = getattr(getattr(s, "status", None), "value", "unknown")
                if name == "agent.run":
                    ag = attrs.get("agent", agent_name)
                    if self._runs is not None:
                        self._runs.add(1, {"agent": ag, "status": status})
                    dur = getattr(s, "duration_ms", None)
                    if dur is not None and self._latency is not None:
                        self._latency.record(float(dur) / 1000.0, {"agent": ag})
                elif name == "llm.complete":
                    provider = attrs.get("provider", "unknown")
                    model = attrs.get("model", "unknown")
                    if self._llm_calls is not None:
                        self._llm_calls.add(1, {"provider": provider, "model": model, "status": status})
                    lat = attrs.get("latency_ms")
                    if lat is not None and self._llm_latency is not None:
                        self._llm_latency.record(float(lat) / 1000.0, {"provider": provider, "model": model})
                    it = int(attrs.get("input_tokens", 0) or 0)
                    ot = int(attrs.get("output_tokens", 0) or 0)
                    if self._tokens is not None:
                        if it > 0:
                            self._tokens.add(it, {"agent": agent_name, "direction": "input"})
                        if ot > 0:
                            self._tokens.add(ot, {"agent": agent_name, "direction": "output"})
                    cost = float(attrs.get("cost", 0.0) or 0.0)
                    if cost > 0 and self._cost is not None:
                        self._cost.add(cost, {"agent": agent_name, "model": model})
                elif name == "tool.call":
                    tool = attrs.get("tool", "unknown")
                    if self._tools is not None:
                        self._tools.add(1, {"tool": tool, "status": status})
        except Exception:
            pass
```

- [ ] **Step 4: Correr el test para verificar que pasa**

Run: `cd /Users/fulfaro/monaccode/astromesh && python3 -m pytest tests/test_engine_metrics.py -v`
Expected: PASS (ambos tests). Si `pytest-asyncio` se queja, estos dos tests son síncronos — no aplica.

- [ ] **Step 5: No-regresión del test de 4.4c**

Run: `cd /Users/fulfaro/monaccode/astromesh && python3 -m pytest tests/test_agent_egress_metric.py -v`
Expected: PASS (el egress counter sigue intacto; `setup()` ahora crea instrumentos extra, sin afectar `record`).

- [ ] **Step 6: Commit (en el repo astromesh)**

```bash
cd /Users/fulfaro/monaccode/astromesh
git add astromesh/observability/metrics_export.py tests/test_engine_metrics.py
git commit -m "feat(metrics): derive full engine metric set from TracingContext (phase4.3b)

Extends the 4.4c MetricsManager from one egress counter to the full set
(agent.runs, agent.latency, llm.calls, llm.latency, agent.tokens,
agent.cost, tool.executions) on the same MeterProvider, plus a
record_run(ctx) that walks the span tree. Best-effort, off by default."
```

---

## Task 2: Cablear `record_run` en `engine.run()` (node)

**Files:**
- Modify: `/Users/fulfaro/monaccode/astromesh/astromesh/runtime/engine.py` (`finally` block ~648-654)
- Test: `/Users/fulfaro/monaccode/astromesh/tests/test_engine_metrics.py` (append)

- [ ] **Step 1: Escribir el test de integración que falla (el engine llama `record_run`)**

Agregar al final de `/Users/fulfaro/monaccode/astromesh/tests/test_engine_metrics.py`:

```python
async def test_engine_calls_record_run(tmp_path):
    from astromesh.observability import metrics_export as mx
    from astromesh.runtime.engine import AgentRuntime

    seen = {"record_run": 0, "agent_run_span": False}

    class CapMgr:
        def record(self, agent, model, n):
            pass

        def record_run(self, ctx):
            seen["record_run"] += 1
            if any(getattr(s, "name", "") == "agent.run" for s in getattr(ctx, "spans", [])):
                seen["agent_run_span"] = True

        def flush(self, timeout_millis=5000):
            pass

    mx.set_manager(CapMgr())

    agents_dir = tmp_path / "agents"; agents_dir.mkdir()
    (agents_dir / "a.agent.yaml").write_text(
        """
apiVersion: astromesh/v1
kind: Agent
metadata: {name: rec-agent, version: "0.1.0", namespace: t}
spec:
  identity: {display_name: A, description: A}
  model: {primary: {provider: ollama, model: m, endpoint: "http://127.0.0.1:1/v1"}}
  prompts: {system: "sys"}
  orchestration: {pattern: react, max_iterations: 1}
"""
    )
    rt = AgentRuntime(config_dir=str(tmp_path)); await rt.bootstrap()
    agent = rt._agents["rec-agent"]

    class Resp:
        model = "m"; provider = "p"; content = "hi"; tool_calls = None
        usage = {"input_tokens": 3, "output_tokens": 2}; latency_ms = 1.0; cost = 0.0

    async def fake_route(messages, tools=None, **kw):
        return Resp()

    agent._router.route = fake_route

    try:
        await rt.run("rec-agent", "hello there", "s1")
    except Exception:
        pass

    assert seen["record_run"] >= 1
    assert seen["agent_run_span"] is True
```

- [ ] **Step 2: Correr el test para verificar que falla**

Run: `cd /Users/fulfaro/monaccode/astromesh && python3 -m pytest tests/test_engine_metrics.py::test_engine_calls_record_run -v`
Expected: FAIL — `assert seen["record_run"] >= 1` falla (el engine aún no llama `record_run`).

- [ ] **Step 3: Cablear la llamada en el `finally` de `engine.run()`**

En `/Users/fulfaro/monaccode/astromesh/astromesh/runtime/engine.py`, en el bloque de 4.4c (busca `_gm2`), agregar `record_run` antes del `flush`:

```python
            # Fase 4.4c: flush the per-agent egress metric (cold gRPC needs a waited flush, like traces).
            try:
                from astromesh.observability.metrics_export import get_manager as _gm2
                _m2 = _gm2()
                if _m2 is not None:
                    _m2.record_run(tracing)   # Fase 4.3b: derive engine metrics from the span tree
                    _m2.flush()
            except Exception:
                logger.debug("agent-egress flush failed", exc_info=True)
```

- [ ] **Step 4: Correr el test para verificar que pasa**

Run: `cd /Users/fulfaro/monaccode/astromesh && python3 -m pytest tests/test_engine_metrics.py -v`
Expected: PASS (los tres tests, incluido el de integración).

- [ ] **Step 5: No-regresión de traces/egress del engine**

Run: `cd /Users/fulfaro/monaccode/astromesh && python3 -m pytest tests/test_agent_egress_metric.py tests/test_otlp_export.py -v`
Expected: PASS (el emit de traces y el egress siguen funcionando; `record_run` es aditivo en el mismo `finally`).

- [ ] **Step 6: Commit (en el repo astromesh)**

```bash
cd /Users/fulfaro/monaccode/astromesh
git add astromesh/runtime/engine.py tests/test_engine_metrics.py
git commit -m "feat(metrics): wire record_run into engine.run() finally (phase4.3b)

Single wiring point next to the 4.3 emit_trace and 4.4c flush; derives
the engine metric set from the completed TracingContext on every run."
```

---

## Task 3: Gate de OS — `tests/boot/otel-metrics-and-assert.sh` (OS)

**Files:**
- Create: `/Users/fulfaro/monaccode/astromesh-os/tests/boot/otel-metrics-and-assert.sh`

- [ ] **Step 1: Crear el gate (clon de `otel-export-and-assert.sh`, asserta métricas)**

Crear `tests/boot/otel-metrics-and-assert.sh` con este contenido exacto:

```bash
#!/usr/bin/env bash
# Fase 4.3b gate: boot ONE VM with an `otel` machine-config (flips observability.otlp.enabled). Assert
# the baked otelcol sidecar is up and that a real agent run's METRICS are EXPORTED to it — the
# collector's debug exporter logs received metrics to the console. Positive: `astromesh.agent.runs`
# (always emitted by any run) reaches the collector; `astromesh.agent.tokens` is asserted too (the
# stub emits usage). Cost is best-effort (the stub may report cost=0), so it is logged, not required.
set -euo pipefail
IMAGE="${1:?usage: otel-metrics-and-assert.sh <v1-raw-image>}"
HERE="$(cd "$(dirname "$0")" && pwd)"
source "${HERE}/lib-smbios.sh"
source "${HERE}/lib-swtpm.sh"

PORT=8000
if [ -w /dev/kvm ]; then ACCEL="-enable-kvm"; SMP=2; else ACCEL="-accel tcg,thread=multi"; SMP=4; fi
OVMF_CODE=""; for c in /usr/share/OVMF/OVMF_CODE_4M.fd /usr/share/OVMF/OVMF_CODE.fd /usr/share/ovmf/OVMF.fd; do
    [ -f "$c" ] && { OVMF_CODE="$c"; break; }
done
OVMF_VARS_SRC=""; for v in /usr/share/OVMF/OVMF_VARS_4M.fd /usr/share/OVMF/OVMF_VARS.fd; do
    [ -f "$v" ] && { OVMF_VARS_SRC="$v"; break; }
done
[ -n "${OVMF_CODE}" ] && [ -n "${OVMF_VARS_SRC}" ] || { echo "[otel] FAIL: OVMF not found"; exit 1; }

TPM="$(mktemp -d)/t"; QPID=""
cleanup() { kill ${QPID} 2>/dev/null || true; SWTPM_DIR="${TPM}" swtpm_stop 2>/dev/null || true; }
trap cleanup EXIT

disk="otelm.qcow2"; vars="ovmf_vars_otelm.fd"; CON="otel-metrics-console.log"
qemu-img convert -O qcow2 "${IMAGE}" "${disk}"; qemu-img resize "${disk}" +3G >/dev/null
cp "${OVMF_VARS_SRC}" "${vars}"
swtpm_start "${TPM}"

CRED_YAML="profile: worker
node_id: otel-metrics-node
otel: true"

# shellcheck disable=SC2046
qemu-system-x86_64 ${ACCEL} -machine q35 -m 2048 -smp ${SMP} -nographic \
    -global ICH9-LPC.disable_s3=1 \
    -drive if=pflash,format=raw,unit=0,readonly=on,file="${OVMF_CODE}" \
    -drive if=pflash,format=raw,unit=1,file="${vars}" \
    -drive file="${disk}",format=qcow2,if=virtio \
    $(swtpm_qemu_args) \
    -smbios "$(smbios_machine_config "${CRED_YAML}")" \
    -nic user,model=virtio-net-pci,hostfwd=tcp::${PORT}-:8000 \
    > "${CON}" 2>&1 &
QPID=$!

echo "[otel] waiting for /v1/health (timeout 420s)"
deadline=$(( $(date +%s) + 420 ))
until curl -fsS "http://localhost:${PORT}/v1/health" >/dev/null 2>&1; do
    if [ "$(date +%s)" -ge "${deadline}" ]; then
        echo "[otel] FAIL: /v1/health never came up"; tail -n 120 "${CON}"; exit 1
    fi
    sleep 3
done

grep -aq "otel\] OTEL-COLLECTOR OK" "${CON}" || { echo "[otel] FAIL: collector not up"; grep -aE 'otel\]' "${CON}" | tail; exit 1; }
echo "[otel] OTEL-COLLECTOR OK (sidecar listening on 127.0.0.1:4317)"

echo "[otel] triggering agent runs (stub provider) to generate engine metrics"
for i in 1 2 3; do
    curl -fsS -X POST "http://localhost:${PORT}/v1/agents/phase0-smoke/run" \
        -H 'Content-Type: application/json' \
        -d '{"query":"otel metrics ping","session_id":"otel-metrics"}' >/dev/null 2>&1 || true
    sleep 1
done

# Assert on the COLLECTOR's debug-exporter metric dump. The debug exporter prints each metric as
# `Name: astromesh.agent.runs` with attributes like `agent: Str(phase0-smoke)` (same format the 4.4c
# gate matches for astromesh.agent.egress.bytes). Require runs (any run emits it) + tokens (the stub
# reports usage). Cost is best-effort.
echo "[otel] asserting engine metrics reached the collector (astromesh.agent.runs + .tokens)"
deadline=$(( $(date +%s) + 120 ))
until grep -aqE 'astromesh\.agent\.runs' "${CON}" \
   && grep -aqE 'agent: Str\(phase0-smoke\)' "${CON}" \
   && grep -aqE 'astromesh\.agent\.tokens' "${CON}"; do
    if [ "$(date +%s)" -ge "${deadline}" ]; then
        echo "[otel] FAIL: engine metrics not seen in the collector dump"
        echo "----- collector metric markers -----"; grep -aE 'astromesh\.(agent|llm|tool)|Name:|agent:|Value:' "${CON}" | tail -n 60
        echo "----- node export markers -----"; grep -aE 'agent-egress metric export enabled|OTLP trace export enabled|agent metrics unavailable' "${CON}" | tail -n 10
        exit 1
    fi
    sleep 3
done
echo "[otel] ENGINE-METRICS-EXPORTED OK (astromesh.agent.runs + .tokens{agent=phase0-smoke} reached the collector)"

if grep -aqE 'astromesh\.agent\.cost' "${CON}"; then
    echo "[otel] (bonus) astromesh.agent.cost also exported"
else
    echo "[otel] note: astromesh.agent.cost not present (stub reported cost=0) — not required by the gate"
fi
echo "[otel] OTEL METRICS GATE PASSED"
```

- [ ] **Step 2: Hacerlo ejecutable y validar la sintaxis bash**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && chmod +x tests/boot/otel-metrics-and-assert.sh && bash -n tests/boot/otel-metrics-and-assert.sh && echo SYNTAX_OK`
Expected: `SYNTAX_OK` (sin errores de parseo).

- [ ] **Step 3: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add tests/boot/otel-metrics-and-assert.sh
git commit -m "test(phase4.3b): 1-VM gate asserts engine metrics reach the collector"
```

---

## Task 4: Target de dev-loop + correr el gate local (la prueba) (OS)

**Files:**
- Modify: `/Users/fulfaro/monaccode/astromesh-os/tests/local/dev-loop.sh` (después del case `otel)`)

- [ ] **Step 1: Agregar el case `otel-metrics)` (clon de `otel)`)**

En `tests/local/dev-loop.sh`, justo después del bloque `otel) … ;;` (el que termina antes de `ebpf-rust)`), insertar:

```bash
  otel-metrics)
    require_kvm; sync_src; ensure_deb; ensure_otelcol
    command -v swtpm >/dev/null 2>&1 || apt-get install -y swtpm
    build_v 1
    cd "${WORKDIR}"
    # 1 VM with an `otel` machine-config; the gate triggers agent runs and asserts the baked otelcol
    # sidecar logged the exported engine metrics (astromesh.agent.runs + .tokens).
    bash tests/boot/otel-metrics-and-assert.sh "mkosi.output/${IMAGE_ID}_1.raw"
    ;;
```

- [ ] **Step 2: Validar la sintaxis del dev-loop**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && bash -n tests/local/dev-loop.sh && echo SYNTAX_OK`
Expected: `SYNTAX_OK`.

- [ ] **Step 3: Correr el gate local (la prueba — requiere WSL2+KVM; usa el .deb rebuildeado localmente con los cambios del nodo)**

> Nota: `ensure_deb` rebuildea el `.deb` desde el checkout local de `../astromesh` (que ya tiene los commits de las Tareas 1–2), así que el gate ve `record_run` sin depender de `runtime.pin`. (El bump de `runtime.pin` para CI es la Tarea 6, igual que 4.3/4.4c.)

Run:
```bash
wsl -d Debian -u root -- bash /mnt/d/monaccode/astromesh-os/tests/local/dev-loop.sh otel-metrics 2>&1 | tail -n 40
```
Expected: `OTEL METRICS GATE PASSED`. Si falla en el assert de métricas, inspeccionar el dump (`astromesh.(agent|llm|tool)` markers) e iterar la forma exacta del nombre/atributo según lo que el debug exporter realmente imprima (nota de discovery del spec §4) — empezar por `astromesh.agent.runs` (garantizado) y ajustar `.tokens`/`.cost` a lo que el stub emita.

- [ ] **Step 4: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add tests/local/dev-loop.sh
git commit -m "test(phase4.3b): dev-loop otel-metrics target (1-VM engine-metrics gate, green)"
```

---

## Task 5: CI — `.github/workflows/phase4-otel-metrics.yml` (OS)

**Files:**
- Create: `/Users/fulfaro/monaccode/astromesh-os/.github/workflows/phase4-otel-metrics.yml`

- [ ] **Step 1: Clonar el workflow de otel y cambiar nombre, trigger y assert final**

Run (clona exacto el workflow base):
```bash
cd /Users/fulfaro/monaccode/astromesh-os
cp .github/workflows/phase4-otel.yml .github/workflows/phase4-otel-metrics.yml
```

- [ ] **Step 2: Editar el clon**

En `.github/workflows/phase4-otel-metrics.yml`, aplicar exactamente estos cambios (el resto del archivo —build del `.deb`, build del `otelcol` vía ocb, build de la imagen— queda idéntico):

1. Primera línea: `name: phase4-otel` → `name: phase4-otel-metrics`.
2. En el bloque `on: push: branches:`, `[feat/phase4.3-otel]` → `[feat/phase4.3b-otel-metrics]`.
3. El paso final que invoca `bash tests/boot/otel-export-and-assert.sh "..."` → cambiar el script a `tests/boot/otel-metrics-and-assert.sh` (mismo argumento de imagen). Si el step tiene `name:`, renombrarlo a algo como `Gate: engine metrics exported to the collector`.

Verificar los tres cambios:
Run: `cd /Users/fulfaro/monaccode/astromesh-os && grep -nE 'name: phase4-otel-metrics|feat/phase4.3b-otel-metrics|otel-metrics-and-assert.sh' .github/workflows/phase4-otel-metrics.yml`
Expected: las tres líneas presentes (nombre, branch, script del gate).

- [ ] **Step 3: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add .github/workflows/phase4-otel-metrics.yml
git commit -m "ci(phase4.3b): workflow for the engine-metrics export gate"
```

---

## Task 6: Roadmap + bump de runtime.pin (OS)

**Files:**
- Modify: `/Users/fulfaro/monaccode/astromesh-os/docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md` (tabla de roadmap, fila 4.3)
- Modify: `/Users/fulfaro/monaccode/astromesh-os/runtime.pin` (cuando los cambios del nodo estén pusheados a `monaccode/astromesh`)

- [ ] **Step 1: Actualizar la fila 4.3 del roadmap**

En `docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`, en la fila `| **4.3** |`, reemplazar el segmento `Métricas → 4.3b` por una referencia a hecho. Editar el final de esa celda de estado para que diga, por ejemplo: `… Métricas → ✅ **4.3b** (2026-06-11) — gate \`OTEL METRICS GATE PASSED\` (set del engine: agent.runs/latency, llm.calls/latency, tokens, cost, tools — derivadas del TracingContext).`

Verificar:
Run: `cd /Users/fulfaro/monaccode/astromesh-os && grep -n "OTEL METRICS GATE PASSED\|4.3b" docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`
Expected: la fila 4.3 menciona 4.3b hecho con el marcador del gate.

- [ ] **Step 2: Bump de `runtime.pin` (solo tras pushear el repo astromesh)**

Una vez que los commits de las Tareas 1–2 estén en `monaccode/astromesh` (push + merge), obtener el nuevo SHA y editar `ASTROMESH_REF` en `runtime.pin`:

```bash
cd /Users/fulfaro/monaccode/astromesh && git rev-parse HEAD
```
Editar `runtime.pin` → `ASTROMESH_REF=<nuevo-sha>`. (Si el push aún no ocurrió, dejar este step pendiente y anotarlo; el gate local no lo necesita porque `ensure_deb` usa el checkout local.)

- [ ] **Step 3: Commit final del roadmap (+ pin si se bumpeo)**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md runtime.pin
git commit -m "docs(phase4.3b): roadmap — engine OTel metrics marked done"
```

---

## Self-Review

**Spec coverage:**
- §1 arquitectura (record_run en el finally, push por pipeline metrics) → Tareas 1–2 (node) + el gate Tarea 3.
- §2.1 set de instrumentos (8 instrumentos, egress conservado) → Tarea 1 Step 3 (a/b).
- §2.2 record_run (recorrido de spans, agent.run/llm.complete/tool.call, labels) → Tarea 1 Step 3 (d) + test Step 1.
- §2.3 cableado de una línea → Tarea 2.
- §2.4 config (reusa observability.otlp, sin campo nuevo) → cubierto (no se toca config; `core.py` ya hace `setup()`+`set_manager`).
- §3 cero cambios de OS-infra → confirmado (Tareas 3–5 solo gate/dev-loop/CI).
- §4 gate (boot, OTEL-COLLECTOR OK, runs+tokens, cost best-effort, GATE PASSED) → Tarea 3 + run local Tarea 4.
- §6 done (local verde, CI, roadmap, off-by-default) → Tareas 4/5/6.
- §7 riesgos (stub cost frágil → assert en runs+tokens; nombre de span de tool → confirmado `tool.call`) → mitigado en Tarea 3 (cost best-effort) y Tarea 1 (tool.call exacto).

**Placeholder scan:** Sin TBD/TODO. La nota de iteración en Tarea 4 Step 3 es un item empírico deliberado (forma exacta del dump del debug exporter) con un assert de arranque concreto (`astromesh.agent.runs`), no un placeholder. El Step 2 de Tarea 6 (bump de pin) es condicional al push, explícitamente marcado.

**Type consistency:** `record_run` (un solo nombre en Tareas 1, 2, y el test). Atributos de instrumentos `_runs/_latency/_llm_calls/_llm_latency/_tokens/_cost/_tools` idénticos en `__init__` (Step 3a), `setup` (3b), el `except` (3c), `record_run` (3d) y los fakes del test (Step 1). Nombres de métricas `astromesh.agent.runs`/`.latency`/`.tokens`/`.cost`, `astromesh.llm.calls`/`.latency`, `astromesh.tool.executions` consistentes entre Tarea 1 (creación), el spec §2.1 y el gate Tarea 3 (grep). Span names `agent.run`/`llm.complete`/`tool.call` coinciden con `engine.py` (verificado: `tool.call` con attr `tool`). `SpanStatus.value` = `"ok"`/`"error"`/`"unset"` (verificado en `tracing.py`), usado en los asserts del test. Consistente.
