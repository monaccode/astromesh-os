# §12.7 — CRIU checkpoint/restore del runtime — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** El OS hace checkpoint del proceso `astromeshd` corriendo y lo restaura, probando en 1 VM que el restore conserva la identidad del proceso (uptime continuo) y la API sigue sirviendo — cero cambios de runtime.

**Architecture:** Un orquestador privilegiado in-VM (oneshot systemd, guardado por `spec.criu.enabled`) hace `criu dump --leave-running` → `systemctl stop astromeshd` → `criu restore -d`, luego asserta uptime continuo (`/v1/system/status.uptime_seconds`, dato RAM que un restart resetea pero CRIU conserva) + una corrida de agente post-restore. El gate host-side solo bootea y grepea `CRIU C/R GATE PASSED`. Sigue el patrón de self-checks de fase 3 y el de unidad-guardada-por-flag de 4.2/4.3.

**Tech Stack:** bash + CRIU (paquete Debian), systemd, QEMU/KVM + mkosi (gate), GitHub Actions. `criu` va en `mkosi.conf` Packages.

**Repo único:** todo en `/Users/fulfaro/monaccode/astromesh-os`. **Cero cambios de runtime** (`../astromesh` no se toca). El "test" de cada componente bash es `bash -n` + el boot gate (Tarea 5), que **corre en WSL2+KVM** (no en macOS) — la corrida real queda documentada para el usuario.

**Spec:** `docs/superpowers/specs/2026-06-11-phase12.7-criu-cr-design.md`.

---

## File Structure

- **`phase4/criu/astromesh-criu-gate.sh`** (new) — orquestador/self-check privilegiado: guard + ciclo dump→stop→restore + asserts + marcadores.
- **`phase4/criu/astromesh-criu-gate.service`** (new) — oneshot privilegiado, `After=astromeshd`, `KillMode=process` (no matar el proceso restaurado al salir).
- **`phase4/machineconfig/astromesh-machine-config.sh`** (mod) — credencial `criu: true` → `spec.criu.enabled: true` en runtime.yaml.
- **`mkosi.conf`** (mod) — `criu` en `Packages=`.
- **`mkosi.postinst.chroot`** (mod) — instala script+unidad, `systemctl enable`.
- **`tests/boot/criu-cr-and-assert.sh`** (new) — gate host-side (boot + grep).
- **`tests/local/dev-loop.sh`** (mod) — target `criu`.
- **`.github/workflows/phase4-criu.yml`** (new) — CI.
- **`docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`** (mod) — fila de roadmap §12.7.

---

## Task 1: Orquestador CRIU (script + unidad)

**Files:**
- Create: `phase4/criu/astromesh-criu-gate.sh`
- Create: `phase4/criu/astromesh-criu-gate.service`

- [ ] **Step 1: Crear el script orquestador**

Crear `phase4/criu/astromesh-criu-gate.sh` con este contenido exacto:

```bash
#!/usr/bin/env bash
# §12.7 CRIU checkpoint/restore self-check/orchestrator. Runs PRIVILEGED (no hardening drop-in), After=
# astromeshd. Guarded: no-op unless the active runtime.yaml has spec.criu.enabled: true. On a criu boot:
#   1. criu dump --tree <pid> --leave-running   (snapshot WITHOUT killing — systemd undisturbed)
#   2. systemctl stop astromeshd                 (clean stop frees :8000 and the PID; no Restart race)
#   3. criu restore -d                           (bring the snapshot back, detached, re-binding :8000)
# Then asserts UPTIME CONTINUITY (/v1/system/status.uptime_seconds does NOT reset — a plain restart would
# give ~0; CRIU keeps _start_time) and FUNCTIONAL CONTINUITY (a post-restore agent run answers). Markers
# to console for the gate harness. The CRIU flags are an iteration item (see spec §7).
set -uo pipefail
log() { echo "[criu] $*"; }
RT=/var/lib/astromesh/runtime.yaml
IMG=/var/lib/astromesh/criu
PORT=8000
BASE="http://127.0.0.1:${PORT}"
PY=/opt/astromesh/venv/bin/python3

# Guard: only run on criu boots (spec.criu.enabled: true in the active runtime.yaml).
en=$("${PY}" - "${RT}" <<'PYEOF' 2>/dev/null || true
import sys, yaml
try:
    d = yaml.safe_load(open(sys.argv[1])) or {}
except Exception:
    d = {}
print(str(((d.get("spec") or {}).get("criu") or {}).get("enabled", "")).strip().lower())
PYEOF
)
if [ "${en}" != "true" ]; then log "no es boot criu; skipping"; exit 0; fi

uptime_of() {
    curl -fsS "${BASE}/v1/system/status" 2>/dev/null \
        | "${PY}" -c 'import sys,json; print(json.load(sys.stdin).get("uptime_seconds",-1))' 2>/dev/null \
        || echo "-1"
}

log "waiting for astromeshd /v1/health (timeout ~240s)"
for _ in $(seq 1 120); do curl -fsS "${BASE}/v1/health" >/dev/null 2>&1 && break; sleep 2; done
curl -fsS "${BASE}/v1/health" >/dev/null 2>&1 || { log "FAIL: astromeshd /v1/health never came up"; exit 1; }

U1=$(uptime_of); log "pre-checkpoint uptime=${U1}"
# warm: an agent run serves PRE-checkpoint (stub provider)
curl -fsS -X POST "${BASE}/v1/agents/phase0-smoke/run" -H 'Content-Type: application/json' \
    -d '{"query":"criu pre","session_id":"criu-1"}' >/dev/null 2>&1 || true

PID=$(systemctl show -p MainPID --value astromeshd.service 2>/dev/null || echo "")
[ -n "${PID}" ] && [ "${PID}" != "0" ] || { log "FAIL: no astromeshd MainPID"; exit 1; }
log "astromeshd MainPID=${PID}"

install -d -m 0700 "${IMG}"; rm -f "${IMG}"/*.img "${IMG}"/*.log 2>/dev/null || true

# 1. DUMP, leaving the process running. Flags are an iteration item (spec §7): external unix sockets
#    (journald stdout, sd_notify), TCP listen socket, file locks. Start here; refine on first run.
if criu dump --tree "${PID}" --leave-running --images-dir "${IMG}" \
        --tcp-established --file-locks --ext-unix-sk --link-remap 2>"${IMG}/dump.log"; then
    log "CRIU-DUMP OK"
else
    log "FAIL: criu dump failed"; tail -n 60 "${IMG}/dump.log" 2>/dev/null || true; exit 1
fi

# 2. Clean stop (frees :8000 and the PID; an intentional stop is NOT a failure, so systemd won't restart).
log "stopping astromeshd.service (clean) before restore"
systemctl stop astromeshd.service 2>/dev/null || true
for _ in $(seq 1 30); do curl -fsS "${BASE}/v1/health" >/dev/null 2>&1 || break; sleep 1; done

# 3. RESTORE detached.
if criu restore -d --images-dir "${IMG}" \
        --tcp-established --file-locks --ext-unix-sk --link-remap 2>"${IMG}/restore.log"; then
    log "CRIU-RESTORE OK"
else
    log "FAIL: criu restore failed"; tail -n 60 "${IMG}/restore.log" 2>/dev/null || true; exit 1
fi

log "waiting for restored astromeshd on :${PORT}"
for _ in $(seq 1 60); do curl -fsS "${BASE}/v1/health" >/dev/null 2>&1 && break; sleep 1; done
curl -fsS "${BASE}/v1/health" >/dev/null 2>&1 \
    || { log "FAIL: restored astromeshd not serving /v1/health"; tail -n 60 "${IMG}/restore.log" 2>/dev/null || true; exit 1; }

U2=$(uptime_of); log "post-restore uptime=${U2}"

# FUNCTIONAL continuity: a post-restore agent run must answer over the restored :8000 socket.
if curl -fsS -X POST "${BASE}/v1/agents/phase0-smoke/run" -H 'Content-Type: application/json' \
        -d '{"query":"criu post","session_id":"criu-2"}' >/dev/null 2>&1; then
    log "POST-RESTORE-RUN OK"
else
    log "FAIL: post-restore agent run did not answer"; exit 1
fi

# UPTIME continuity: U2 must be >= U1 (a plain restart resets _start_time -> uptime ~0).
cont=$("${PY}" -c "import sys; print('yes' if float('${U2}')>=float('${U1}') and float('${U1}')>=0 else 'no')" 2>/dev/null || echo "no")
if [ "${cont}" = "yes" ]; then
    log "UPTIME-CONTINUOUS OK (U1=${U1} -> U2=${U2}, not reset)"
else
    log "FAIL: uptime reset (U1=${U1} -> U2=${U2}) — restored process is not the checkpointed one"; exit 1
fi

log "CRIU C/R GATE PASSED"
```

- [ ] **Step 2: Crear la unidad systemd**

Crear `phase4/criu/astromesh-criu-gate.service` con este contenido exacto:

```ini
[Unit]
Description=Astromesh OS §12.7 CRIU checkpoint/restore self-check (guarded by spec.criu.enabled)
After=astromeshd.service astromesh-machine-config.service
Wants=astromeshd.service

[Service]
Type=oneshot
RemainAfterExit=yes
# KillMode=process: when this oneshot exits, do NOT kill the cgroup — the CRIU-restored astromeshd is a
# detached child and must keep running (the MVP restores outside systemd's astromeshd.service unit).
KillMode=process
ExecStart=/usr/lib/astromesh-os/criu/astromesh-criu-gate.sh
StandardOutput=journal+console
StandardError=journal+console

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 3: Validar sintaxis bash + hacer ejecutable**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && chmod +x phase4/criu/astromesh-criu-gate.sh && bash -n phase4/criu/astromesh-criu-gate.sh && echo SYNTAX_OK`
Expected: `SYNTAX_OK`.

- [ ] **Step 4: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add phase4/criu/astromesh-criu-gate.sh phase4/criu/astromesh-criu-gate.service
git commit -m "feat(12.7): privileged CRIU C/R orchestrator (dump --leave-running -> stop -> restore)"
```

---

## Task 2: machine-config — credencial `criu: true` → `spec.criu.enabled`

**Files:**
- Modify: `phase4/machineconfig/astromesh-machine-config.sh`

- [ ] **Step 1: Leer el bloque de la credencial `otel` para clonar su forma**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && grep -n "otel=\|ebpf_egress=\|criu" phase4/machineconfig/astromesh-machine-config.sh`
Esto ubica los bloques de flag existentes (`otel`, `ebpf_egress`). El nuevo bloque `criu` los imita: lee la credencial, y si es `true`/`1` fija `spec.criu.enabled: true` en `${RUNTIME_YAML}`.

- [ ] **Step 2: Insertar el bloque `criu`**

En `phase4/machineconfig/astromesh-machine-config.sh`, **después** del bloque `ebpf_egress` (busca su `fi` de cierre — el bloque que setea `spec.ebpf.egress`) y antes del final del script, insertar:

```bash
# §12.7: if the machine-config asks for CRIU checkpoint/restore (criu: true), set the runtime flag so the
# guarded astromesh-criu-gate unit runs the C/R cycle on this boot. Same pattern as the 4.3 otel flag.
criu=$(/opt/astromesh/venv/bin/python3 - "${CRED}" <<'PY'
import sys, yaml
d = yaml.safe_load(open(sys.argv[1])) or {}
print(str(d.get("criu", "")).strip().lower())
PY
)
if [ "${criu}" = "true" ] || [ "${criu}" = "1" ]; then
    /opt/astromesh/venv/bin/python3 - "${RUNTIME_YAML}" <<'PY'
import sys, yaml
path = sys.argv[1]
d = yaml.safe_load(open(path)) or {}
d.setdefault("spec", {}).setdefault("criu", {})["enabled"] = True
yaml.safe_dump(d, open(path, "w"), sort_keys=False)
PY
    log "criu C/R enabled (spec.criu.enabled=true)"
fi
```

(Match the file's existing indentation and the `${CRED}` / `${RUNTIME_YAML}` variables already defined near the top — `CRED` is the credential path, `RUNTIME_YAML=/var/lib/astromesh/runtime.yaml`. If the real `ebpf_egress` block's closing differs from what you expect, place the new block right after its final `fi`, mirroring the surrounding style; report what you found.)

- [ ] **Step 3: Validar sintaxis**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && bash -n phase4/machineconfig/astromesh-machine-config.sh && echo SYNTAX_OK`
Expected: `SYNTAX_OK`.

- [ ] **Step 4: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add phase4/machineconfig/astromesh-machine-config.sh
git commit -m "feat(12.7): machine-config criu:true -> spec.criu.enabled (gates the C/R unit)"
```

---

## Task 3: Hornear `criu` + instalar la unidad (mkosi)

**Files:**
- Modify: `mkosi.conf` (Packages)
- Modify: `mkosi.postinst.chroot` (install + enable)

- [ ] **Step 1: Agregar `criu` a `Packages=` en `mkosi.conf`**

En `mkosi.conf`, dentro de la lista `Packages=` (después de la última entrada, p.ej. tras `libzstd1` o `ethtool`), agregar:

```
        # §12.7: CRIU for runtime checkpoint/restore. Only used on criu boots (the astromesh-criu-gate
        # unit is guarded by spec.criu.enabled); the binary is inert otherwise. ~5 MB with its deps.
        criu
```

Verificar: `cd /Users/fulfaro/monaccode/astromesh-os && grep -n "^        criu$" mkosi.conf`
Expected: una línea (la entrada `criu` en Packages).

- [ ] **Step 2: Instalar el script+unidad y habilitarla en `mkosi.postinst.chroot`**

En `mkosi.postinst.chroot`, **después** del bloque de instalación de la machine-config (busca `systemctl enable astromesh-machine-config.service`, ~línea 253), insertar:

```bash
# §12.7: CRIU checkpoint/restore self-check. Privileged oneshot (no hardening drop-in), guarded by
# spec.criu.enabled in the active runtime.yaml — inert on non-criu boots. `criu` itself is baked via
# mkosi Packages. enable is unconditional (the unit self-guards), mirroring the machine-config unit.
install -d /usr/lib/astromesh-os/criu
install -m 0755 "${SRC}/phase4/criu/astromesh-criu-gate.sh" /usr/lib/astromesh-os/criu/astromesh-criu-gate.sh
install -m 0644 "${SRC}/phase4/criu/astromesh-criu-gate.service" /etc/systemd/system/astromesh-criu-gate.service
systemctl enable astromesh-criu-gate.service
```

(Use the same `${SRC}` variable and `install`/`systemctl enable` idiom as the surrounding blocks — confirm `${SRC}` is the repo-root variable used by the other `install -m ... "${SRC}/phase4/..."` lines.)

- [ ] **Step 3: Validar sintaxis de ambos**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && bash -n mkosi.postinst.chroot && echo POSTINST_OK`
Expected: `POSTINST_OK`. (`mkosi.conf` is INI-like, not bash — no syntax check; the Packages grep in Step 1 is its check.)

- [ ] **Step 4: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add mkosi.conf mkosi.postinst.chroot
git commit -m "build(12.7): bake criu (Packages) + install/enable the guarded C/R unit"
```

---

## Task 4: Gate host-side (`tests/boot/criu-cr-and-assert.sh`)

**Files:**
- Create: `tests/boot/criu-cr-and-assert.sh`

- [ ] **Step 1: Crear el gate (clon del scaffolding de `otel-export-and-assert.sh`, asserta el marcador de CRIU)**

Crear `tests/boot/criu-cr-and-assert.sh` con este contenido exacto:

```bash
#!/usr/bin/env bash
# §12.7 gate: boot ONE VM with a `criu: true` machine-config (flips spec.criu.enabled). The in-guest
# astromesh-criu-gate oneshot runs the C/R cycle (dump --leave-running -> systemctl stop astromeshd ->
# restore -d) and asserts uptime continuity + a post-restore agent run, printing CRIU markers to the
# console. This host-side gate just boots and greps for `CRIU C/R GATE PASSED`.
set -euo pipefail
IMAGE="${1:?usage: criu-cr-and-assert.sh <v1-raw-image>}"
HERE="$(cd "$(dirname "$0")" && pwd)"
source "${HERE}/lib-smbios.sh"
source "${HERE}/lib-swtpm.sh"

PORT=8000
if [ -w /dev/kvm ]; then ACCEL="-enable-kvm"; SMP=2; else ACCEL="-accel tcg,thread=multi"; SMP=4; fi
OVMF_CODE=""; for c in /usr/share/OVMF/OVMF_CODE_4M.fd /usr/share/OVMF/OVMF_CODE.fd /usr/share/ovmf/OVMF.fd; do
    [ -f "$c" ] && { OVMF_CODE="$c"; break; }
done
OVMF_VARS_SRC=""; for v in /usr/share/OVMF/OVMF_VARS_4M.fd /usr/share/OVMF/OVMF_VARS.fd; do
    [ -f "$v" ] && { OVMF_VARS_SRC="$v"; break; }
done
[ -n "${OVMF_CODE}" ] && [ -n "${OVMF_VARS_SRC}" ] || { echo "[criu] FAIL: OVMF not found"; exit 1; }

TPM="$(mktemp -d)/t"; QPID=""
cleanup() { kill ${QPID} 2>/dev/null || true; SWTPM_DIR="${TPM}" swtpm_stop 2>/dev/null || true; }
trap cleanup EXIT

disk="criu.qcow2"; vars="ovmf_vars_criu.fd"; CON="criu-console.log"
qemu-img convert -O qcow2 "${IMAGE}" "${disk}"; qemu-img resize "${disk}" +3G >/dev/null
cp "${OVMF_VARS_SRC}" "${vars}"
swtpm_start "${TPM}"

CRED_YAML="profile: worker
node_id: criu-node
criu: true"

# shellcheck disable=SC2046
qemu-system-x86_64 ${ACCEL} -machine q35 -m 2048 -smp ${SMP} -nographic \
    -global ICH9-LPC.disable_s3=1 \
    -drive if=pflash,format=raw,unit=0,readonly=on,file="${OVMF_CODE}" \
    -drive if=pflash,format=raw,unit=1,file="${vars}" \
    -drive file="${disk}",format=qcow2,if=virtio \
    $(swtpm_qemu_args) \
    -smbios "$(smbios_machine_config "${CRED_YAML}")" \
    -nic user,model=virtio-net-pci,hostfwd=tcp::${PORT}-:8000 \
    > "${CON}" 2>&1 &
QPID=$!

echo "[criu] waiting for /v1/health (timeout 420s)"
deadline=$(( $(date +%s) + 420 ))
until curl -fsS "http://localhost:${PORT}/v1/health" >/dev/null 2>&1; do
    if [ "$(date +%s)" -ge "${deadline}" ]; then
        echo "[criu] FAIL: /v1/health never came up"; tail -n 120 "${CON}"; exit 1
    fi
    sleep 3
done

echo "[criu] asserting the in-guest C/R cycle dumped+restored astromeshd (give it time)"
deadline=$(( $(date +%s) + 240 ))
until grep -aqE 'CRIU C/R GATE PASSED' "${CON}"; do
    if [ "$(date +%s)" -ge "${deadline}" ]; then
        echo "[criu] FAIL: CRIU C/R cycle did not pass in the guest"
        echo "----- criu markers -----"; grep -aE 'criu\]' "${CON}" | tail -n 60
        exit 1
    fi
    if grep -aqE 'criu\] FAIL:' "${CON}"; then
        echo "[criu] FAIL: the in-guest orchestrator reported a failure"
        grep -aE 'criu\]' "${CON}" | tail -n 60
        exit 1
    fi
    sleep 3
done
echo "[criu] CRIU-DUMP/RESTORE + uptime-continuity + post-restore-run all OK in the guest"
echo "[criu] CRIU C/R GATE PASSED"
```

- [ ] **Step 2: Ejecutable + sintaxis**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && chmod +x tests/boot/criu-cr-and-assert.sh && bash -n tests/boot/criu-cr-and-assert.sh && echo SYNTAX_OK`
Expected: `SYNTAX_OK`. Confirmar que `tests/boot/lib-smbios.sh` y `tests/boot/lib-swtpm.sh` existen (`ls tests/boot/lib-smbios.sh tests/boot/lib-swtpm.sh`) — no modificarlos.

- [ ] **Step 3: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add tests/boot/criu-cr-and-assert.sh
git commit -m "test(12.7): 1-VM gate boots a criu node and asserts CRIU C/R GATE PASSED"
```

---

## Task 5: dev-loop target + CI workflow

**Files:**
- Modify: `tests/local/dev-loop.sh`
- Create: `.github/workflows/phase4-criu.yml`

- [ ] **Step 1: Agregar el case `criu)` al dev-loop**

Primero leer el case `boot)` / `otel)` para imitar la forma (los targets de 1 VM sin otelcol). En `tests/local/dev-loop.sh`, después del case `otel-metrics)` (o cualquier case de 1 VM; ubicalo con `grep -n 'otel-metrics)\|ebpf-rust)' tests/local/dev-loop.sh`), insertar:

```bash
  criu)
    require_kvm; sync_src; ensure_deb
    command -v swtpm >/dev/null 2>&1 || apt-get install -y swtpm
    build_v 1
    cd "${WORKDIR}"
    # 1 VM with a `criu: true` machine-config; the in-guest astromesh-criu-gate oneshot dumps+restores
    # astromeshd and the gate asserts uptime continuity + a post-restore agent run.
    bash tests/boot/criu-cr-and-assert.sh "mkosi.output/${IMAGE_ID}_1.raw"
    ;;
```

(Note: NO `ensure_otelcol` — the criu gate does not need the collector. Mirror the exact helper calls of a non-otel 1-VM target like `boot)`/`sandbox)`: `require_kvm; sync_src; ensure_deb; ... build_v 1`. If those targets use a different helper sequence, match it.)

Also add `criu` to the `*)` unknown-target usage string (`die "unknown target ... (use: ... | otel-metrics | ... | criu | inspect | clean)"`), placed consistently.

- [ ] **Step 2: Validar sintaxis del dev-loop**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && bash -n tests/local/dev-loop.sh && echo SYNTAX_OK`
Expected: `SYNTAX_OK`.

- [ ] **Step 3: Crear el workflow de CI (clon de `phase4-otel.yml`, SIN el paso ocb del otelcol)**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
cp .github/workflows/phase4-otel.yml .github/workflows/phase4-criu.yml
```

Luego editar `.github/workflows/phase4-criu.yml`:
1. `name: phase4-otel` → `name: phase4-criu`.
2. `branches: [feat/phase4.3-otel]` → `branches: [feat/phase12.7-criu]`.
3. **Eliminar el step de build del otelcol vía ocb** (el step con `go run go.opentelemetry.io/collector/cmd/builder` / "Build minimal OTel Collector"): el gate de CRIU no usa el collector. (Si quitar el step deja un `dist/otelcol` referenciado más abajo, no pasa nada — el postinst hornea otelcol solo `if [ -x dist/otelcol ]`; sin ese archivo, no se hornea, que es lo correcto para un build criu.)
4. El step final del gate: `bash tests/boot/otel-export-and-assert.sh "<image-arg>"` → `bash tests/boot/criu-cr-and-assert.sh "<image-arg>"` (mismo argumento de imagen). Si el step tiene `name:`, reescribirlo (p.ej. `name: "Gate: CRIU checkpoint/restore round-trip"` — entre comillas por el `:`).

Verificar: `cd /Users/fulfaro/monaccode/astromesh-os && grep -nE 'name: phase4-criu|feat/phase12.7-criu|criu-cr-and-assert.sh' .github/workflows/phase4-criu.yml` (tres matches) y `grep -n 'otel-export-and-assert.sh\|cmd/builder\|feat/phase4.3-otel' .github/workflows/phase4-criu.yml` (sin matches). Si `python3` + pyyaml están: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/phase4-criu.yml'))" && echo YAML_OK`.

- [ ] **Step 4: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add tests/local/dev-loop.sh .github/workflows/phase4-criu.yml
git commit -m "test(12.7): dev-loop criu target + CI workflow (1-VM C/R gate)"
```

- [ ] **Step 5: Correr el gate local (la prueba — REQUIERE WSL2+KVM; no corre en macOS)**

> macOS no tiene KVM/mkosi/qemu; este step lo corre el usuario en WSL2. CRIU + el ciclo no son viables bajo TCG.

Run (desde el host Windows):
```bash
wsl -d Debian -u root -- bash /mnt/d/monaccode/astromesh-os/tests/local/dev-loop.sh criu 2>&1 | tail -n 50
```
Expected: `CRIU C/R GATE PASSED`. **Iteración esperada (estilo 4.4e):** si `criu dump`/`restore` falla, leer `dump.log`/`restore.log` (la consola los vuelca en el FAIL) y ajustar los flags de CRIU en `astromesh-criu-gate.sh` (typ.: agregar `--external` para el socket de journald, `--manage-cgroups=ignore`, o `--shell-job` no aplica). Empezar por el set actual (`--tcp-established --file-locks --ext-unix-sk --link-remap`) y refinar hasta verde. Documentar el set final que funcionó.

---

## Task 6: Roadmap

**Files:**
- Modify: `docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`

- [ ] **Step 1: Localizar la fila post-4 / §12.7**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && grep -n "12.7\|post-4\|4.5\|CRIU\|sched_ext" docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`
Si existe una fila/nota post-4 que enumera §12.7 (snapshot/restore CRIU) como pendiente, editarla a hecho. Si NO existe una fila para §12.7 en la tabla de este doc (la tabla cubre 4.1–4.5), agregar una fila nueva al final de la tabla de roadmap:

```
| §12.7 | **CRIU checkpoint/restore** del runtime (post-4, F) | C/R del proceso astromeshd con estado vivo | ✅ **HECHO** (2026-06-11) — gate `CRIU C/R GATE PASSED` (dump --leave-running → stop → restore; uptime continuo + corrida post-restore). Orquestador privilegiado in-VM; restore detached (re-adopción a systemd → futuro). |
```

Match the table's column count (cuenta los `|` de las filas vecinas y replicalos — la tabla tiene 4 columnas / 5 pipes; ver fila 4.5 como molde). Mantener una sola línea por fila.

- [ ] **Step 2: Verificar integridad de la tabla**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && grep -n "CRIU C/R GATE PASSED\|12.7" docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`
Expected: la fila §12.7 presente con el marcador del gate. Confirmar que la fila nueva tiene el mismo número de `|` que las vecinas (tabla no rota).

- [ ] **Step 3: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md
git commit -m "docs(12.7): roadmap — CRIU checkpoint/restore marked done"
```

---

## Self-Review

**Spec coverage:**
- §0.1 discovery (unidad = daemon, mono-proceso, sandbox → helper externo, uptime discriminator) → Task 1 (orquestador externo privilegiado, usa `/v1/system/status.uptime_seconds`).
- §1 arquitectura (oneshot guardado, no hardening, KillMode=process) → Task 1 Step 2 (unidad).
- §1.1 ciclo dump --leave-running → systemctl stop → restore -d → Task 1 Step 1 (script).
- §2 flujo del gate (guard, health, U1, warm run, dump, stop, restore, U2, post-run, asserts, marcador) → Task 1 Step 1, 1:1.
- §3 componentes: script+unidad (Task 1), machine-config criu flag (Task 2), `criu` en Packages + postinst (Task 3), gate (Task 4), dev-loop+CI (Task 5).
- §4 gate host-side (boot cred criu:true, health, grep CRIU C/R GATE PASSED) → Task 4.
- §6 done (local verde, CI, gates previos verdes, roadmap) → Task 5 (run) + Task 6.
- §7 riesgos (flags de CRIU iteración; sockets externos; kernel; restore detached; seccomp) → Task 1 (flags de arranque + comentario de iteración), Task 5 Step 5 (iteración documentada).

**Placeholder scan:** Sin TBD/TODO. La iteración de flags de CRIU (Task 5 Step 5) es un ítem empírico deliberado con set de arranque concreto, estilo 4.4e — no un placeholder. La condicionalidad de la fila de roadmap (Task 6 Step 1: editar-si-existe / agregar-si-no) es por incertidumbre real sobre si el doc ya tiene fila post-4; ambos caminos están especificados.

**Type/contract consistency:** Flag de runtime `spec.criu.enabled` idéntico en Task 2 (machine-config lo escribe) y Task 1 (el guard lo lee). Credencial `criu: true` consistente en Task 2 (parse) y Task 4/5 (CRED_YAML del gate). Marcadores `CRIU-DUMP OK`/`CRIU-RESTORE OK`/`CRIU C/R GATE PASSED` consistentes entre el orquestador (Task 1) y los greps del gate (Task 4) y la expectativa del dev-loop (Task 5). Ruta del script `/usr/lib/astromesh-os/criu/astromesh-criu-gate.sh` idéntica en la unidad (Task 1 Step 2 ExecStart) y el postinst (Task 3 Step 2 install). Endpoint `/v1/system/status` + campo `uptime_seconds` verificados contra `astromesh/api/routes/system.py` + `main.py` (`include_router(system.router, prefix="/v1")`, router prefix `/system`, `@router.get("/status")`, `uptime_seconds`). Agente `phase0-smoke` consistente con los gates otel/egress. Consistente.
