# §12.3 — Gobierno de memoria por cgroup — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Acotar el blast-radius de un agente que fuga memoria al cgroup de `astromeshd` (cgroup-OOM contenido + recuperación determinista por systemd) en lugar del OOM-killer del host arrasando el nodo — cero cambios de runtime.

**Architecture:** Un drop-in **always-on** en `astromeshd.service` (`MemoryAccounting` + `MemoryMax`/`MemoryHigh` en %, `OOMPolicy=kill`, `Restart=on-failure`) + un self-check **fail-closed** que astromeshd `Requires=` (patrón 3.4 sandbox 1:1): verifica las directivas y corre un POSITIVE-OOM (un hog en un scope transitorio `MemoryMax=64M` queda contenido). El gate host-side bootea 1 VM estándar y grepea los marcadores.

**Tech Stack:** systemd (cgroup v2 memory controller, `systemd-run --scope`), bash, QEMU/KVM + mkosi (gate), GitHub Actions.

**Repo único:** todo en `/Users/fulfaro/monaccode/astromesh-os`. **Cero cambios de runtime.** El "test" de cada componente es `bash -n` + el boot gate (Tarea 3/4), que **corre en WSL2+KVM** (no en macOS) — la corrida real y la verificación de que los gates previos siguen verdes quedan documentadas para el usuario.

**Spec:** `docs/superpowers/specs/2026-06-11-phase12.3-memory-governance-design.md`.

**Branch:** crear `feat/phase12.3-memory` desde `main` antes de empezar (la implementación NO va sobre `feat/phase12.7-criu`).

---

## File Structure

- **`phase4/memory/50-memory.conf`** (new) — drop-in en `astromeshd.service.d/`: directivas de memoria + `Requires=` el check.
- **`phase4/memory/astromesh-memory-check.sh`** (new) — self-check fail-closed: directivas + POSITIVE-OOM + marcadores `[memory]`.
- **`phase4/memory/astromesh-memory-check.service`** (new) — oneshot, `Before=astromeshd`, sin hardening (necesita privilegio pleno para `systemd-run`/cgroups).
- **`mkosi.postinst.chroot`** (mod) — instala drop-in + script + unidad, enable.
- **`tests/boot/memory-oom-and-assert.sh`** (new) — gate host-side (boot estándar + grep).
- **`tests/local/dev-loop.sh`** (mod) — target `memory`.
- **`.github/workflows/phase4-memory.yml`** (new) — CI.
- **`docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`** (mod) — fila de roadmap §12.3.

---

## Task 1: Drop-in + self-check + unidad

**Files:**
- Create: `phase4/memory/50-memory.conf`
- Create: `phase4/memory/astromesh-memory-check.sh`
- Create: `phase4/memory/astromesh-memory-check.service`

- [ ] **Step 1: Crear el drop-in `phase4/memory/50-memory.conf`**

```ini
[Unit]
# §12.3 memory governance — always-on hardening (like 30-sandbox.conf / 40-egress.conf). Fail-closed:
# astromeshd Requires= the self-check, which runs FIRST and verifies the kernel enforces cgroup memory
# limits. If it can't, the check fails -> Requires= fails -> astromeshd does not start.
Requires=astromesh-memory-check.service
After=astromesh-memory-check.service

[Service]
MemoryAccounting=yes
# Percentages of physical RAM -> scale with VM size. astromeshd uses ~200 MB, far below 70/80%
# (1.4/1.6 GB on a 2 GB VM), so normal workloads are unaffected; only a runaway is capped.
MemoryHigh=70%
MemoryMax=80%
# No swap to mask the limit.
MemorySwapMax=0
# Deterministic recovery: a cgroup-OOM kill marks the unit failed -> systemd restarts it.
OOMPolicy=kill
Restart=on-failure
RestartSec=2
```

- [ ] **Step 2: Crear el self-check `phase4/memory/astromesh-memory-check.sh`**

```bash
#!/usr/bin/env bash
# §12.3 memory governance self-check. Runs BEFORE astromeshd (which Requires= this unit via
# 50-memory.conf). Fail-closed: if memory governance isn't configured OR the kernel doesn't enforce
# cgroup memory limits, this exits non-zero -> astromeshd's Requires= fails -> astromeshd does not start.
# Asserts: (1) astromeshd.service has MemoryAccounting=yes + a finite MemoryMax + OOMPolicy=kill;
# (2) POSITIVE-OOM: a 512M hog in a transient MemoryMax=64M scope is cgroup-OOM-contained (cannot
# complete) while this check and the host survive. Markers [memory] to console for the gate harness.
set -uo pipefail
log() { echo "[memory] $*"; }
fail=0

ACC=$(systemctl show astromeshd.service -p MemoryAccounting --value 2>/dev/null || echo "")
MAX=$(systemctl show astromeshd.service -p MemoryMax --value 2>/dev/null || echo "")
OOM=$(systemctl show astromeshd.service -p OOMPolicy --value 2>/dev/null || echo "")
log "astromeshd MemoryAccounting=${ACC:-?} MemoryMax=${MAX:-?} OOMPolicy=${OOM:-?}"

if [ "${ACC}" = "yes" ]; then log "MEMORY-ACCOUNTING OK"; else log "FAIL: MemoryAccounting not enabled (${ACC:-none})"; fail=1; fi
# Unset MemoryMax == "infinity"; a configured percentage resolves to a finite byte count.
if [ -n "${MAX}" ] && [ "${MAX}" != "infinity" ] && [ "${MAX}" -gt 0 ] 2>/dev/null; then
    log "MEMORYMAX-SET OK (MemoryMax=${MAX} bytes, OOMPolicy=${OOM})"
else
    log "FAIL: MemoryMax not a finite limit (${MAX:-none})"; fail=1
fi
if [ "${OOM}" != "kill" ]; then log "FAIL: OOMPolicy is '${OOM:-none}', expected kill"; fail=1; fi

# POSITIVE-OOM: a single 512M allocation under a transient MemoryMax=64M scope (no swap) cannot succeed —
# the kernel cgroup-OOM-kills it. rc != 0 == contained; rc == 0 == the limit did NOT enforce.
log "POSITIVE-OOM: running a 512M hog under a transient MemoryMax=64M scope (must be contained)"
rc=0
systemd-run --scope --quiet -p MemoryMax=64M -p MemorySwapMax=0 \
    /opt/astromesh/venv/bin/python3 -c 'b = b"x" * (512 * 1024 * 1024)' >/dev/null 2>&1 || rc=$?
if [ "${rc}" -ne 0 ]; then
    log "POSITIVE-OOM OK (512M hog contained by the cgroup MemoryMax, rc=${rc}; check + host survived)"
else
    log "FAIL: 512M hog COMPLETED under a 64M cgroup limit — kernel not enforcing memory.max"; fail=1
fi

if [ "${fail}" -ne 0 ]; then log "MEMORY FAILED"; exit 1; fi
log "MEMORY GATE OK"
```

- [ ] **Step 3: Crear la unidad `phase4/memory/astromesh-memory-check.service`**

```ini
[Unit]
Description=Astromesh OS §12.3 memory governance self-check (cgroup MemoryMax + POSITIVE-OOM, fail-closed)
# Runs BEFORE astromeshd, which Requires= this unit (see 50-memory.conf): if memory governance can't be
# verified, THIS unit fails -> astromeshd's Requires= fails -> astromeshd does not start (fail-closed).
Before=astromeshd.service

[Service]
Type=oneshot
ExecStart=/usr/lib/astromesh-os/astromesh-memory-check.sh
StandardOutput=journal+console
StandardError=journal+console
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
```

(NOTA: a diferencia del sandbox-check, esta unidad NO lleva directivas de hardening — necesita privilegio pleno para `systemd-run --scope` y leer cgroups. Es un system unit por default = root, sin sandbox.)

- [ ] **Step 4: Hacer ejecutable el script + validar sintaxis bash**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && chmod +x phase4/memory/astromesh-memory-check.sh && bash -n phase4/memory/astromesh-memory-check.sh && echo SYNTAX_OK`
Expected: `SYNTAX_OK`. Si `bash -n` falla, corregir solo el artefacto de sintaxis y reportar.

- [ ] **Step 5: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add phase4/memory/50-memory.conf phase4/memory/astromesh-memory-check.sh phase4/memory/astromesh-memory-check.service
git commit -m "feat(12.3): memory-governance drop-in (MemoryMax/OOMPolicy) + fail-closed self-check"
```

---

## Task 2: Instalar el drop-in + script + unidad (postinst)

**Files:**
- Modify: `mkosi.postinst.chroot`

- [ ] **Step 1: Leer el bloque de install del sandbox para clonar la forma**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && grep -n "30-sandbox.conf\|astromesh-sandbox-check\|astromeshd.service.d" mkosi.postinst.chroot`
El bloque del sandbox (3.4) instala su drop-in en `astromeshd.service.d/`, su script en `/usr/lib/astromesh-os/`, y su unidad + enable. Tu bloque de memoria lo imita.

- [ ] **Step 2: Insertar el bloque de install de memoria**

En `mkosi.postinst.chroot`, **después** del bloque de install del sandbox (busca `systemctl enable astromesh-sandbox-check.service`), insertar:

```bash

# §12.3: memory governance. Always-on cgroup memory limits on astromeshd (MemoryMax/High, OOMPolicy=kill,
# Restart) via a drop-in, plus a fail-closed self-check that verifies the kernel enforces cgroup memory
# limits (POSITIVE-OOM). Same shape as the 3.4 sandbox: drop-in + Requires=-gated self-check.
install -d /etc/systemd/system/astromeshd.service.d
install -m 0644 "${SRC}/phase4/memory/50-memory.conf" /etc/systemd/system/astromeshd.service.d/50-memory.conf
install -m 0755 "${SRC}/phase4/memory/astromesh-memory-check.sh" /usr/lib/astromesh-os/astromesh-memory-check.sh
install -m 0644 "${SRC}/phase4/memory/astromesh-memory-check.service" /etc/systemd/system/astromesh-memory-check.service
systemctl enable astromesh-memory-check.service
```

(Usar la misma variable `${SRC}` que los otros `install -m ... "${SRC}/phase..."`. Los paths de install DEBEN coincidir con la unidad de Task 1: el script va a `/usr/lib/astromesh-os/astromesh-memory-check.sh` —el `ExecStart` de la unidad—, la unidad a `/etc/systemd/system/astromesh-memory-check.service`, el drop-in a `astromeshd.service.d/50-memory.conf`. Insertar SOLO este bloque.)

- [ ] **Step 3: Validar sintaxis**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && bash -n mkosi.postinst.chroot && echo POSTINST_OK`
Expected: `POSTINST_OK`.

- [ ] **Step 4: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add mkosi.postinst.chroot
git commit -m "build(12.3): install the memory drop-in + fail-closed self-check unit"
```

---

## Task 3: Gate host-side (`tests/boot/memory-oom-and-assert.sh`)

**Files:**
- Create: `tests/boot/memory-oom-and-assert.sh`

- [ ] **Step 1: Crear el gate (clon de `tests/boot/sandbox-and-assert.sh`, asserta los marcadores de memoria)**

Crear `tests/boot/memory-oom-and-assert.sh` con este contenido exacto:

```bash
#!/usr/bin/env bash
# §12.3 gate: boot the standard image, assert astromeshd is functional WITH the always-on memory
# governance (/v1/health 200 — which also proves the fail-closed memory self-check passed, since
# astromeshd Requires= it), then assert the in-guest self-check markers on the console: MemoryAccounting +
# a finite MemoryMax + OOMPolicy=kill, and the POSITIVE-OOM containment.
# Usage: memory-oom-and-assert.sh <qcow2-disk-image>
set -euo pipefail

IMAGE="${1:?usage: memory-oom-and-assert.sh <disk-image>}"
PORT=8000
qemu-img resize "${IMAGE}" +3G >/dev/null

if [ -w /dev/kvm ]; then ACCEL="-enable-kvm"; SMP=2; else ACCEL="-accel tcg,thread=multi"; SMP=4; fi

OVMF_CODE=""
for c in /usr/share/OVMF/OVMF_CODE_4M.fd /usr/share/OVMF/OVMF_CODE.fd /usr/share/ovmf/OVMF.fd; do
    [ -f "$c" ] && { OVMF_CODE="$c"; break; }
done
OVMF_VARS_SRC=""
for v in /usr/share/OVMF/OVMF_VARS_4M.fd /usr/share/OVMF/OVMF_VARS.fd; do
    [ -f "$v" ] && { OVMF_VARS_SRC="$v"; break; }
done
[ -n "${OVMF_CODE}" ] && [ -n "${OVMF_VARS_SRC}" ] || { echo "[memory] FAIL: OVMF not found"; exit 1; }
cp "${OVMF_VARS_SRC}" ovmf_vars_memory.fd

echo "[memory] starting QEMU"
qemu-system-x86_64 \
    ${ACCEL} -machine q35 -m 2048 -smp ${SMP} -nographic \
    -drive if=pflash,format=raw,unit=0,readonly=on,file="${OVMF_CODE}" \
    -drive if=pflash,format=raw,unit=1,file=ovmf_vars_memory.fd \
    -drive file="${IMAGE}",format=qcow2,if=virtio \
    -nic user,model=virtio-net-pci,hostfwd=tcp::${PORT}-:${PORT} \
    > memory-console.log 2>&1 &
QPID=$!
trap 'kill ${QPID} 2>/dev/null || true' EXIT

echo "[memory] waiting for /v1/health (timeout 240s) — proves memory governance didn't break the runtime"
deadline=$(( $(date +%s) + 240 ))
until curl -fsS "http://localhost:${PORT}/v1/health" >/dev/null 2>&1; do
    if [ "$(date +%s)" -ge "${deadline}" ]; then
        echo "[memory] FAIL: /v1/health never came up under memory governance"; tail -n 120 memory-console.log; exit 1
    fi
    sleep 3
done
echo "[memory] PASS: /v1/health 200 (astromeshd started -> the fail-closed memory self-check passed)"

echo "[memory] asserting in-guest memory self-check marker"
deadline=$(( $(date +%s) + 60 ))
until grep -aq 'memory\] MEMORY GATE OK' memory-console.log; do
    if grep -aq 'memory\] MEMORY FAILED' memory-console.log; then
        echo "[memory] FAIL: self-check reported MEMORY FAILED"
        grep -aE 'memory\]' memory-console.log | tail -20; exit 1
    fi
    if [ "$(date +%s)" -ge "${deadline}" ]; then
        echo "[memory] FAIL: memory self-check marker not seen"
        grep -aE 'memory\]' memory-console.log | tail -20; exit 1
    fi
    sleep 3
done
echo "[memory] PASS: memory self-check (accounting + finite MemoryMax + OOMPolicy=kill + POSITIVE-OOM)"
echo "[memory] MEMORY GATE PASSED"
```

- [ ] **Step 2: Ejecutable + sintaxis**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && chmod +x tests/boot/memory-oom-and-assert.sh && bash -n tests/boot/memory-oom-and-assert.sh && echo SYNTAX_OK`
Expected: `SYNTAX_OK`.

- [ ] **Step 3: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add tests/boot/memory-oom-and-assert.sh
git commit -m "test(12.3): 1-VM gate asserts memory governance + POSITIVE-OOM containment"
```

---

## Task 4: dev-loop target + CI workflow

**Files:**
- Modify: `tests/local/dev-loop.sh`
- Create: `.github/workflows/phase4-memory.yml`

- [ ] **Step 1: Agregar el case `memory)` al dev-loop (clon del case `sandbox)`)**

Primero leer el case `sandbox)` (`grep -n -A9 "sandbox)" tests/local/dev-loop.sh`) para imitar su forma exacta (convierte el raw a qcow2 y pasa el qcow2). En `tests/local/dev-loop.sh`, después del case `sandbox)` (o cualquier case de boot estándar de 1 VM), insertar:

```bash
  memory)
    require_kvm; sync_src; ensure_deb
    # Same stale-server guard as sandbox: a leftover :HTTP_PORT server would auto-update this v1 and
    # reboot before the memory self-check runs.
    pkill -f "http.server ${HTTP_PORT}" 2>/dev/null || true
    build_v 1
    cd "${WORKDIR}"
    qemu-img convert -O qcow2 "mkosi.output/${IMAGE_ID}_1.raw" memory.qcow2
    bash tests/boot/memory-oom-and-assert.sh memory.qcow2
    ;;
```

(Clonar la forma REAL del case `sandbox)` — mismos helpers `require_kvm; sync_src; ensure_deb`, el guard `pkill`, `build_v 1`, `cd "${WORKDIR}"`, la conversión `qemu-img convert -O qcow2 ... <name>.qcow2`, y la invocación del gate. Solo cambian el nombre del qcow2 y el script del gate. Si el case `sandbox)` real difiere, mirrorearlo.)

Agregar también `memory` al string de usage del `*)` default (`die "unknown target ... | sandbox | ... | memory | ...)"`), placed consistently.

- [ ] **Step 2: Validar sintaxis del dev-loop**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && bash -n tests/local/dev-loop.sh && echo SYNTAX_OK`
Expected: `SYNTAX_OK`.

- [ ] **Step 3: Crear el CI (clon de `phase4-otel.yml`, SIN el step ocb del otelcol)**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
cp .github/workflows/phase4-otel.yml .github/workflows/phase4-memory.yml
```

Editar `.github/workflows/phase4-memory.yml`:
1. `name: phase4-otel` → `name: phase4-memory`.
2. `branches: [feat/phase4.3-otel]` → `branches: [feat/phase12.3-memory]`.
3. **Eliminar el step de build del otelcol vía ocb** (el `- name: ...` que corre `go run go.opentelemetry.io/collector/cmd/builder` e instala `dist/otelcol`) — el gate de memoria no usa el collector.
4. El step final del gate: `bash tests/boot/otel-export-and-assert.sh "<image-arg>"` → convertir el raw a qcow2 y correr el gate de memoria. Reemplazar la línea `run:` del step del gate por:
   ```yaml
       run: |
         qemu-img convert -O qcow2 "<image-arg>" memory.qcow2
         bash tests/boot/memory-oom-and-assert.sh memory.qcow2
   ```
   donde `<image-arg>` es exactamente el path de imagen que usaba el step de otel (p.ej. `image/v1.raw`). Si el step tiene `name:`, reescribirlo a `"Gate: memory governance + POSITIVE-OOM"` (entre comillas por el `:`).
5. **Reescribir el comentario NOTE del header** (heredado de otel, habla de "OTLP export / node-source change" — engañoso): reemplazarlo por una nota de que §12.3 es OS-puro (sin cambio de runtime), el gate prueba el gobierno de memoria por cgroup + POSITIVE-OOM, y necesita KVM.

Verificar: `grep -nE 'name: phase4-memory|feat/phase12.3-memory|memory-oom-and-assert.sh' .github/workflows/phase4-memory.yml` (tres matches) y `grep -n 'otel-export-and-assert.sh\|cmd/builder\|otelcol\|feat/phase4.3-otel' .github/workflows/phase4-memory.yml` (sin matches). Si pyyaml está: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/phase4-memory.yml'))" && echo YAML_OK`.

- [ ] **Step 4: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add tests/local/dev-loop.sh .github/workflows/phase4-memory.yml
git commit -m "test(12.3): dev-loop memory target + CI workflow (1-VM memory-governance gate)"
```

- [ ] **Step 5: Correr el gate local (la prueba — REQUIERE WSL2+KVM; no corre en macOS)**

> macOS no tiene KVM/mkosi/qemu; lo corre el usuario en WSL2.

Run (desde el host Windows):
```bash
wsl -d Debian -u root -- bash /mnt/d/monaccode/astromesh-os/tests/local/dev-loop.sh memory 2>&1 | tail -n 40
```
Expected: `MEMORY GATE PASSED`. **Iteración esperada (estilo 4.4e):** si el POSITIVE-OOM no se comporta como se espera, ajustar la invocación de `systemd-run` en `astromesh-memory-check.sh` (típ.: `--scope` vs `--service`, agregar `--wait`, o leer `memory.events:oom_kill` del scope para una aserción más fuerte que `rc != 0`). El hog de 512M bajo `MemoryMax=64M` sin swap debe quedar contenido. Documentar la forma que funcionó.

- [ ] **Step 6: Verificar que los gates previos siguen verdes (always-on)** — en WSL/CI

> El drop-in es always-on, así que TODO boot ahora corre bajo `MemoryMax=80%` + el `Requires=memory-check`. Verificar que no rompe boots existentes:
```bash
wsl -d Debian -u root -- bash /mnt/d/monaccode/astromesh-os/tests/local/dev-loop.sh boot 2>&1 | tail -n 15
wsl -d Debian -u root -- bash /mnt/d/monaccode/astromesh-os/tests/local/dev-loop.sh sandbox 2>&1 | tail -n 15
```
Expected: ambos siguen verdes (astromeshd arranca con el drop-in aplicado; RSS ≪ 80%). Si alguno falla porque astromeshd no arranca, revisar el self-check (probable `systemd-run`/cgroup) — es fail-closed, así que un check roto bloquea TODO boot. Documentar.

---

## Task 5: Roadmap

**Files:**
- Modify: `docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`

- [ ] **Step 1: Localizar la tabla de roadmap y la fila §12.7 (molde)**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && grep -n "§12.7\|12.3\|sysext-gpu\|^| " docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`
La tabla tiene 4 columnas (5 pipes). La fila `§12.7` (agregada antes) es el molde exacto de estructura para una fila post-4.

- [ ] **Step 2: Agregar la fila §12.3**

Agregar al final de la tabla de roadmap (después de la fila `§12.7`, o junto a las filas post-4), como una sola línea, con el mismo número de `|` que las vecinas:

```
| §12.3 | **Gobierno de memoria por cgroup** (post-4, F) — MemoryMax/OOMPolicy en astromeshd, blast-radius acotado | drop-in always-on + self-check fail-closed; POSITIVE-OOM contenido | ✅ **HECHO** (2026-06-11) — gate `MEMORY GATE PASSED` (accounting + MemoryMax finito + OOMPolicy=kill + hog de 512M contenido por cgroup). |
```

(Contar los `|` de la fila `§12.7` y replicar exactamente. Una sola línea.)

- [ ] **Step 3: Verificar integridad de la tabla**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && grep -n "MEMORY GATE PASSED\|§12.3" docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`
Expected: la fila §12.3 presente con el marcador. Confirmar (contando `|`) que tiene el mismo número de separadores que las vecinas.

- [ ] **Step 4: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md
git commit -m "docs(12.3): roadmap — memory governance marked done"
```

---

## Self-Review

**Spec coverage:**
- §1.1 drop-in (MemoryAccounting + MemoryMax/High % + MemorySwapMax + OOMPolicy=kill + Restart + Requires=) → Task 1 Step 1, 1:1.
- §1.2 self-check (3 aserciones: accounting, MemoryMax finito + OOMPolicy, POSITIVE-OOM vía systemd-run) → Task 1 Step 2, 1:1.
- §2 gate (boot estándar, /v1/health prueba fail-closed, grep markers) → Task 3.
- §3 componentes (drop-in, check, unit, postinst, gate, dev-loop, CI) → Tasks 1–4.
- §5 done (local verde, CI, gates previos verdes, always-on) → Task 4 Steps 5–6 + Task 5.
- §6 riesgos (always-on no rompe previos → Task 4 Step 6; systemd-run iteración → Task 4 Step 5; OOM vs MemoryError → POSITIVE-OOM `rc != 0` con nota de iteración).

**Placeholder scan:** Sin TBD/TODO. La iteración de `systemd-run` (Task 4 Step 5) y la verificación de gates previos (Step 6) son ítems empíricos deliberados de WSL, con asserts/comandos concretos — no placeholders.

**Type/contract consistency:** Marcadores `MEMORY-ACCOUNTING OK`/`MEMORYMAX-SET OK`/`POSITIVE-OOM OK`/`MEMORY GATE OK` (in-VM, Task 1) ↔ el gate grepea `MEMORY GATE OK` (pass) y `MEMORY FAILED` (bail) (Task 3) — consistente; el host imprime `MEMORY GATE PASSED` como conclusión propia (patrón sandbox: in-VM `SANDBOX GATE OK`, host `SANDBOX GATE PASSED`). Ruta del script `/usr/lib/astromesh-os/astromesh-memory-check.sh` idéntica en la unidad (Task 1 Step 3 ExecStart) y el postinst (Task 2 Step 2 install). Unidad `astromesh-memory-check.service` referenciada idéntica en el drop-in `Requires=` (Task 1 Step 1), la unidad (Task 1 Step 3), y el postinst enable (Task 2). Drop-in `50-memory.conf` → `astromeshd.service.d/50-memory.conf` consistente Task 1/Task 2. `systemd-run --scope -p MemoryMax=64M` + el hog `b"x"*(512*1024*1024)` coherentes con el assert `rc != 0`. Consistente.
