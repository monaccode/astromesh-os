# §12.2a — sched_ext (scheduler de CPU custom) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** El OS puede cargar un scheduler de CPU custom vía sched_ext (un scheduler BPF mínimo de la suite `scx`), gated por credencial, probando que queda activo (`/sys/kernel/sched_ext/state == enabled`) y que `astromeshd` sigue sirviendo bajo él — cero cambios de runtime.

**Architecture:** Patrón capability-gated (otel/criu) + loader privilegiado (4.4d): una unidad `Type=exec` guardada por `spec.sched_ext.enabled` corre un scheduler `scx` en foreground (mientras vive, el scheduler BPF está attached; si muere, el kernel revierte a CFS por el watchdog), y un self-check oneshot verifica SUPPORTED/ACTIVE/FUNCTIONAL y emite marcadores `[schedext]`. El gate host-side bootea 1 VM con `sched_ext: true` y grepea.

**Tech Stack:** sched_ext (cgroup/kernel BPF struct_ops), la suite `scx` (paquete Debian), systemd, bash, QEMU/KVM + mkosi, GitHub Actions.

**Repo único:** todo en `/Users/fulfaro/monaccode/astromesh-os`. **Cero cambios de runtime.** El "test" de cada componente es `bash -n` + el boot gate (Tarea 4/5), que **corre en WSL2+KVM** (no en macOS).

**Branch:** crear `feat/phase12.2a-schedext` desde `main` antes de empezar.

**DECISIÓN DE PROVISIONING (refina spec §2):** el binario del scheduler se obtiene del **paquete Debian `scx`** (`apt`, horneado vía mkosi Packages — patrón criu), NO como build-input. El loader **auto-detecta** un binario de scheduler de una lista conocida (`scx_simple scx_bpfland scx_rusty scx_lavd`), corriendo el primero disponible — robusto a qué provee el paquete. **Fallback (si en WSL el paquete `scx` no existe en trixie o no trae un scheduler usable):** buildear `scx_simple` desde el repo `sched-ext/scx` como build-input `dist/scx_simple` (spec §2 original) — documentado en la Tarea 3 Step 4.

---

## File Structure

- **`phase4/schedext/astromesh-schedext.sh`** (new) — wrapper del loader: guard + auto-detecta el binario scx + `exec` en foreground.
- **`phase4/schedext/astromesh-schedext.service`** (new) — loader gated privilegiado (`Type=exec`).
- **`phase4/schedext/astromesh-schedext-check.sh`** (new) — self-check gated: SUPPORTED/ACTIVE/FUNCTIONAL + marcadores.
- **`phase4/schedext/astromesh-schedext-check.service`** (new) — oneshot, `After=` el loader.
- **`phase4/machineconfig/astromesh-machine-config.sh`** (mod) — `sched_ext: true` → `spec.sched_ext.enabled`.
- **`mkosi.conf`** (mod) — `scx` en Packages.
- **`mkosi.postinst.chroot`** (mod) — instala scripts + unidades, enable.
- **`tests/boot/schedext-and-assert.sh`** (new) — gate host-side.
- **`tests/local/dev-loop.sh`** (mod) — target `schedext`.
- **`.github/workflows/phase4-schedext.yml`** (new) — CI.
- **`docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`** (mod) — fila de roadmap §12.2a.

---

## Task 1: machine-config — `sched_ext: true` → `spec.sched_ext.enabled`

**Files:**
- Modify: `phase4/machineconfig/astromesh-machine-config.sh`

- [ ] **Step 1: Leer el bloque `criu` para clonar su forma**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && grep -n "criu=\|otel=\|spec.criu\|RUNTIME_YAML\|CRED=" phase4/machineconfig/astromesh-machine-config.sh`
El bloque `criu` (`spec.criu.enabled`) es el molde exacto. (Nota: el bloque `criu` está en el branch `feat/phase12.7-criu`, NO en main; si no aparece, usar el bloque `otel` —`spec.observability.otlp.enabled`— como molde, misma forma.)

- [ ] **Step 2: Insertar el bloque `sched_ext`**

En `phase4/machineconfig/astromesh-machine-config.sh`, después del último bloque de flag (busca el `fi` del bloque `ebpf_egress` o `otel`), insertar:

```bash
# §12.2a: if the machine-config asks for sched_ext (sched_ext: true), set the runtime flag so the guarded
# schedext loader+check run on this boot. Same pattern as the 4.3 otel flag.
sched_ext=$(/opt/astromesh/venv/bin/python3 - "${CRED}" <<'PY'
import sys, yaml
d = yaml.safe_load(open(sys.argv[1])) or {}
print(str(d.get("sched_ext", "")).strip().lower())
PY
)
if [ "${sched_ext}" = "true" ] || [ "${sched_ext}" = "1" ]; then
    /opt/astromesh/venv/bin/python3 - "${RUNTIME_YAML}" <<'PY'
import sys, yaml
path = sys.argv[1]
d = yaml.safe_load(open(path)) or {}
d.setdefault("spec", {}).setdefault("sched_ext", {})["enabled"] = True
yaml.safe_dump(d, open(path, "w"), sort_keys=False)
PY
    log "sched_ext enabled (spec.sched_ext.enabled=true)"
fi
```

(Match the file's indentation; `${CRED}`/`${RUNTIME_YAML}`/`log` are the existing variables/function used by the `otel`/`ebpf_egress` blocks — use them as-is.)

- [ ] **Step 3: Validar sintaxis**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && bash -n phase4/machineconfig/astromesh-machine-config.sh && echo SYNTAX_OK`
Expected: `SYNTAX_OK`.

- [ ] **Step 4: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add phase4/machineconfig/astromesh-machine-config.sh
git commit -m "feat(12.2a): machine-config sched_ext:true -> spec.sched_ext.enabled"
```

---

## Task 2: Loader + self-check (scripts + unidades)

**Files:**
- Create: `phase4/schedext/astromesh-schedext.sh`
- Create: `phase4/schedext/astromesh-schedext.service`
- Create: `phase4/schedext/astromesh-schedext-check.sh`
- Create: `phase4/schedext/astromesh-schedext-check.service`

- [ ] **Step 1: Crear el wrapper del loader `phase4/schedext/astromesh-schedext.sh`**

```bash
#!/usr/bin/env bash
# §12.2a sched_ext loader wrapper. Guarded: no-op unless the active runtime.yaml has
# spec.sched_ext.enabled: true. On a schedext boot it auto-detects an scx scheduler binary (from the
# baked `scx` package) and execs it in the FOREGROUND — while this process lives, the BPF scheduler is
# attached; if it dies, the kernel's sched_ext watchdog reverts to CFS. Runs privileged (loading a
# struct_ops BPF scheduler needs full caps), so this unit carries NO hardening drop-in.
set -uo pipefail
log() { echo "[schedext] $*"; }
RT=/var/lib/astromesh/runtime.yaml
PY=/opt/astromesh/venv/bin/python3

en=$("${PY}" - "${RT}" <<'PYEOF' 2>/dev/null || true
import sys, yaml
try:
    d = yaml.safe_load(open(sys.argv[1])) or {}
except Exception:
    d = {}
print(str(((d.get("spec") or {}).get("sched_ext") or {}).get("enabled", "")).strip().lower())
PYEOF
)
if [ "${en}" != "true" ]; then log "no es boot schedext; skipping"; exit 0; fi

# Auto-detect an scx scheduler binary (the Debian `scx` package may ship any of these).
SCHED=""
for cand in scx_simple scx_bpfland scx_rusty scx_lavd; do
    if command -v "${cand}" >/dev/null 2>&1; then SCHED="${cand}"; break; fi
done
[ -n "${SCHED}" ] || { log "FAIL: no scx scheduler binary found on PATH (scx package not baked?)"; exit 1; }

log "loading sched_ext scheduler: ${SCHED}"
exec "${SCHED}"
```

- [ ] **Step 2: Crear la unidad del loader `phase4/schedext/astromesh-schedext.service`**

```ini
[Unit]
Description=Astromesh OS §12.2a sched_ext loader (guarded by spec.sched_ext.enabled; runs an scx scheduler)
After=astromesh-machine-config.service
Before=astromesh-schedext-check.service

[Service]
Type=exec
ExecStart=/usr/lib/astromesh-os/schedext/astromesh-schedext.sh
# While this process runs, the BPF scheduler is attached. On exit, the kernel reverts to CFS.
Restart=no
StandardOutput=journal+console
StandardError=journal+console

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 3: Crear el self-check `phase4/schedext/astromesh-schedext-check.sh`**

```bash
#!/usr/bin/env bash
# §12.2a sched_ext self-check. Guarded (no-op unless spec.sched_ext.enabled). Runs After= the loader.
# Asserts: (1) the kernel supports sched_ext (/sys/kernel/sched_ext exists), (2) a custom scheduler is
# ACTIVE (state == enabled + a non-empty ops name), (3) astromeshd is functional under it (/v1/health
# 200). Markers [schedext] to console for the gate harness.
set -uo pipefail
log() { echo "[schedext] $*"; }
RT=/var/lib/astromesh/runtime.yaml
PY=/opt/astromesh/venv/bin/python3
fail=0

en=$("${PY}" - "${RT}" <<'PYEOF' 2>/dev/null || true
import sys, yaml
try:
    d = yaml.safe_load(open(sys.argv[1])) or {}
except Exception:
    d = {}
print(str(((d.get("spec") or {}).get("sched_ext") or {}).get("enabled", "")).strip().lower())
PYEOF
)
if [ "${en}" != "true" ]; then log "no es boot schedext; skipping"; exit 0; fi

# 1. SUPPORTED: the kernel has CONFIG_SCHED_CLASS_EXT (the sysfs dir exists).
if [ -d /sys/kernel/sched_ext ]; then
    log "SCHEDEXT-SUPPORTED OK (/sys/kernel/sched_ext present)"
else
    log "FAIL: /sys/kernel/sched_ext missing — kernel lacks CONFIG_SCHED_CLASS_EXT"; log "SCHEDEXT FAILED"; exit 1
fi

# 2. ACTIVE: a custom scheduler is loaded. Give the loader a moment to attach.
state=""; ops=""
for _ in $(seq 1 30); do
    state=$(cat /sys/kernel/sched_ext/state 2>/dev/null || echo "")
    ops=$(cat /sys/kernel/sched_ext/root/ops 2>/dev/null || echo "")
    [ "${state}" = "enabled" ] && [ -n "${ops}" ] && break
    sleep 1
done
log "sched_ext state=${state:-?} ops=${ops:-?}"
if [ "${state}" = "enabled" ] && [ -n "${ops}" ]; then
    log "SCHEDEXT-ACTIVE OK (custom scheduler '${ops}' is the active CPU scheduler)"
else
    log "FAIL: no custom sched_ext scheduler active (state=${state:-none} ops=${ops:-none})"; fail=1
fi

# 3. FUNCTIONAL: astromeshd still serves under the custom scheduler.
ok=0
for _ in $(seq 1 60); do
    if curl -fsS "http://127.0.0.1:8000/v1/health" >/dev/null 2>&1; then ok=1; break; fi
    sleep 2
done
if [ "${ok}" -eq 1 ]; then
    log "RUNTIME-FUNCTIONAL OK (astromeshd /v1/health 200 under the custom scheduler)"
else
    log "FAIL: astromeshd /v1/health did not come up under the custom scheduler"; fail=1
fi

if [ "${fail}" -ne 0 ]; then log "SCHEDEXT FAILED"; exit 1; fi
log "SCHEDEXT GATE OK"
```

- [ ] **Step 4: Crear la unidad del check `phase4/schedext/astromesh-schedext-check.service`**

```ini
[Unit]
Description=Astromesh OS §12.2a sched_ext self-check (SUPPORTED + ACTIVE + FUNCTIONAL)
After=astromesh-schedext.service astromeshd.service
Wants=astromesh-schedext.service

[Service]
Type=oneshot
ExecStart=/usr/lib/astromesh-os/schedext/astromesh-schedext-check.sh
StandardOutput=journal+console
StandardError=journal+console
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 5: Ejecutables + sintaxis**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && chmod +x phase4/schedext/astromesh-schedext.sh phase4/schedext/astromesh-schedext-check.sh && bash -n phase4/schedext/astromesh-schedext.sh && bash -n phase4/schedext/astromesh-schedext-check.sh && echo SYNTAX_OK`
Expected: `SYNTAX_OK`.

- [ ] **Step 6: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add phase4/schedext/astromesh-schedext.sh phase4/schedext/astromesh-schedext.service phase4/schedext/astromesh-schedext-check.sh phase4/schedext/astromesh-schedext-check.service
git commit -m "feat(12.2a): guarded sched_ext loader (scx) + self-check (SUPPORTED/ACTIVE/FUNCTIONAL)"
```

---

## Task 3: Hornear `scx` + instalar las unidades (mkosi)

**Files:**
- Modify: `mkosi.conf`
- Modify: `mkosi.postinst.chroot`

- [ ] **Step 1: Agregar `scx` a `Packages=` en `mkosi.conf`**

En `mkosi.conf`, dentro de `Packages=` (después del último entry, p.ej. `criu`/`linux-image-cloud-amd64`), agregar:

```
        # §12.2a: scx — the sched_ext scheduler suite (provides scx_simple/scx_bpfland/...). Only used on
        # sched_ext boots (the astromesh-schedext loader is guarded by spec.sched_ext.enabled); inert
        # otherwise. The 6.12+ Debian cloud kernel ships CONFIG_SCHED_CLASS_EXT (ADR-3).
        scx
```

Verificar: `cd /Users/fulfaro/monaccode/astromesh-os && grep -nE "^        scx$" mkosi.conf` → una línea.

- [ ] **Step 2: Instalar scripts + unidades en `mkosi.postinst.chroot`**

Después del bloque de install del sandbox (`grep -n "systemctl enable astromesh-sandbox-check.service" mkosi.postinst.chroot`), insertar:

```bash

# §12.2a: sched_ext. Guarded loader (runs an scx scheduler when spec.sched_ext.enabled) + self-check.
# `scx` is baked via mkosi Packages. enable is unconditional (the units self-guard), like the otel units.
install -d /usr/lib/astromesh-os/schedext
install -m 0755 "${SRC}/phase4/schedext/astromesh-schedext.sh" /usr/lib/astromesh-os/schedext/astromesh-schedext.sh
install -m 0755 "${SRC}/phase4/schedext/astromesh-schedext-check.sh" /usr/lib/astromesh-os/schedext/astromesh-schedext-check.sh
install -m 0644 "${SRC}/phase4/schedext/astromesh-schedext.service" /etc/systemd/system/astromesh-schedext.service
install -m 0644 "${SRC}/phase4/schedext/astromesh-schedext-check.service" /etc/systemd/system/astromesh-schedext-check.service
systemctl enable astromesh-schedext.service astromesh-schedext-check.service
```

(Usar `${SRC}`. Rutas de los scripts DEBEN coincidir con los `ExecStart` de las unidades: `/usr/lib/astromesh-os/schedext/astromesh-schedext.sh` y `…-check.sh`.)

- [ ] **Step 3: Validar sintaxis**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && bash -n mkosi.postinst.chroot && echo POSTINST_OK`
Expected: `POSTINST_OK`.

- [ ] **Step 4: (Discovery / fallback — para tener presente en WSL, NO bloquea el commit)**

> El `scx` package puede no existir en trixie o no traer un scheduler usable. Resolución empírica en el primer build/gate de WSL:
> - Si `mkosi build` falla en el apt de `scx`: confirmar el nombre real del paquete (`apt-cache search sched-ext|scx` en un trixie chroot) — candidatos: `scx`, `scx-scheds`. Ajustar la línea de `Packages`.
> - Si el paquete no existe: FALLBACK al build-input (spec §2) — buildear `scx_simple` desde `github.com/sched-ext/scx` (meson + clang + libbpf + bpftool) en host/CI, stagearlo a `dist/scx_simple`, hornearlo condicional en postinst (`if [ -x dist/scx_simple ]` → `/usr/bin/scx_simple`), y agregar `ensure_scx` al dev-loop (espejo de `ensure_ebpf_rust`). El loader ya auto-detecta `scx_simple` en PATH, así que no cambia.
>
> Documentar la resolución (paquete vs build-input) cuando se corra en WSL.

- [ ] **Step 5: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add mkosi.conf mkosi.postinst.chroot
git commit -m "build(12.2a): bake the scx scheduler suite + install/enable the guarded schedext units"
```

---

## Task 4: Gate host-side (`tests/boot/schedext-and-assert.sh`)

**Files:**
- Create: `tests/boot/schedext-and-assert.sh`

- [ ] **Step 1: Leer un gate con machine-config (smbios + swtpm) para clonar**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && sed -n '1,60p' tests/boot/otel-export-and-assert.sh`
El gate de schedext necesita la credencial `sched_ext: true` vía SMBIOS, así que clona el scaffolding de `otel-export-and-assert.sh` (lib-smbios + lib-swtpm + el `-smbios "$(smbios_machine_config ...)"`).

- [ ] **Step 2: Crear `tests/boot/schedext-and-assert.sh`**

```bash
#!/usr/bin/env bash
# §12.2a gate: boot ONE VM with a `sched_ext: true` machine-config (flips spec.sched_ext.enabled). The
# in-guest astromesh-schedext loader runs an scx scheduler and the self-check asserts the kernel supports
# sched_ext, a custom scheduler is ACTIVE, and astromeshd is functional under it. This host-side gate
# boots and greps for `SCHEDEXT GATE OK`.
set -euo pipefail
IMAGE="${1:?usage: schedext-and-assert.sh <v1-raw-image>}"
HERE="$(cd "$(dirname "$0")" && pwd)"
source "${HERE}/lib-smbios.sh"
source "${HERE}/lib-swtpm.sh"

PORT=8000
if [ -w /dev/kvm ]; then ACCEL="-enable-kvm"; SMP=2; else ACCEL="-accel tcg,thread=multi"; SMP=4; fi
OVMF_CODE=""; for c in /usr/share/OVMF/OVMF_CODE_4M.fd /usr/share/OVMF/OVMF_CODE.fd /usr/share/ovmf/OVMF.fd; do
    [ -f "$c" ] && { OVMF_CODE="$c"; break; }
done
OVMF_VARS_SRC=""; for v in /usr/share/OVMF/OVMF_VARS_4M.fd /usr/share/OVMF/OVMF_VARS.fd; do
    [ -f "$v" ] && { OVMF_VARS_SRC="$v"; break; }
done
[ -n "${OVMF_CODE}" ] && [ -n "${OVMF_VARS_SRC}" ] || { echo "[schedext] FAIL: OVMF not found"; exit 1; }

TPM="$(mktemp -d)/t"; QPID=""
cleanup() { kill ${QPID} 2>/dev/null || true; SWTPM_DIR="${TPM}" swtpm_stop 2>/dev/null || true; }
trap cleanup EXIT

disk="schedext.qcow2"; vars="ovmf_vars_schedext.fd"; CON="schedext-console.log"
qemu-img convert -O qcow2 "${IMAGE}" "${disk}"; qemu-img resize "${disk}" +3G >/dev/null
cp "${OVMF_VARS_SRC}" "${vars}"
swtpm_start "${TPM}"

CRED_YAML="profile: worker
node_id: schedext-node
sched_ext: true"

# shellcheck disable=SC2046
qemu-system-x86_64 ${ACCEL} -machine q35 -m 2048 -smp ${SMP} -nographic \
    -global ICH9-LPC.disable_s3=1 \
    -drive if=pflash,format=raw,unit=0,readonly=on,file="${OVMF_CODE}" \
    -drive if=pflash,format=raw,unit=1,file="${vars}" \
    -drive file="${disk}",format=qcow2,if=virtio \
    $(swtpm_qemu_args) \
    -smbios "$(smbios_machine_config "${CRED_YAML}")" \
    -nic user,model=virtio-net-pci,hostfwd=tcp::${PORT}-:8000 \
    > "${CON}" 2>&1 &
QPID=$!

echo "[schedext] waiting for /v1/health (timeout 420s)"
deadline=$(( $(date +%s) + 420 ))
until curl -fsS "http://localhost:${PORT}/v1/health" >/dev/null 2>&1; do
    if [ "$(date +%s)" -ge "${deadline}" ]; then
        echo "[schedext] FAIL: /v1/health never came up"; tail -n 120 "${CON}"; exit 1
    fi
    sleep 3
done

echo "[schedext] asserting the in-guest sched_ext self-check (SUPPORTED + ACTIVE + FUNCTIONAL)"
deadline=$(( $(date +%s) + 180 ))
until grep -aqE 'schedext\] SCHEDEXT GATE OK' "${CON}"; do
    if grep -aqE 'schedext\] SCHEDEXT FAILED' "${CON}"; then
        echo "[schedext] FAIL: in-guest self-check reported SCHEDEXT FAILED"
        grep -aE 'schedext\]' "${CON}" | tail -n 40; exit 1
    fi
    if [ "$(date +%s)" -ge "${deadline}" ]; then
        echo "[schedext] FAIL: SCHEDEXT GATE OK not seen"
        grep -aE 'schedext\]' "${CON}" | tail -n 40; exit 1
    fi
    sleep 3
done
echo "[schedext] custom scheduler ACTIVE + astromeshd FUNCTIONAL under it"
echo "[schedext] SCHEDEXT GATE PASSED"
```

- [ ] **Step 3: Ejecutable + sintaxis + helpers**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && chmod +x tests/boot/schedext-and-assert.sh && bash -n tests/boot/schedext-and-assert.sh && echo SYNTAX_OK && ls tests/boot/lib-smbios.sh tests/boot/lib-swtpm.sh`
Expected: `SYNTAX_OK` + ambos helpers presentes (no modificarlos).

- [ ] **Step 4: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add tests/boot/schedext-and-assert.sh
git commit -m "test(12.2a): 1-VM gate boots a sched_ext node and asserts a custom scheduler is active"
```

---

## Task 5: dev-loop target + CI workflow

**Files:**
- Modify: `tests/local/dev-loop.sh`
- Create: `.github/workflows/phase4-schedext.yml`

- [ ] **Step 1: Agregar el case `schedext)` al dev-loop**

Leer el case `otel)` (`grep -n -A8 "  otel)" tests/local/dev-loop.sh`) para imitar (necesita swtpm para el boot con smbios). En `tests/local/dev-loop.sh`, después del case `otel)` (o `criu)`/cualquier case smbios+swtpm de 1 VM), insertar:

```bash
  schedext)
    require_kvm; sync_src; ensure_deb
    command -v swtpm >/dev/null 2>&1 || apt-get install -y swtpm
    build_v 1
    cd "${WORKDIR}"
    # 1 VM with a `sched_ext: true` machine-config; the in-guest loader runs an scx scheduler and the
    # gate asserts a custom CPU scheduler is active + astromeshd functional under it.
    bash tests/boot/schedext-and-assert.sh "mkosi.output/${IMAGE_ID}_1.raw"
    ;;
```

(NO usa `ensure_otelcol`/`ensure_ebpf_rust`/`ensure_scx`: el scheduler viene del paquete `scx` horneado, no de un build-input. Mirror la forma real del case `otel)`; si difiere, clonarlo y cambiar solo el gate script. Si el fallback de la Tarea 3 Step 4 se activa —build-input de scx_simple—, agregar `ensure_scx` acá espejo de `ensure_ebpf_rust`.)

Agregar `schedext` al string de usage del `*)` default.

- [ ] **Step 2: Validar sintaxis**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && bash -n tests/local/dev-loop.sh && echo SYNTAX_OK`
Expected: `SYNTAX_OK`.

- [ ] **Step 3: Crear el CI (clon de `phase4-otel.yml`, SIN el step ocb del otelcol)**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
cp .github/workflows/phase4-otel.yml .github/workflows/phase4-schedext.yml
```

Editar `.github/workflows/phase4-schedext.yml`:
1. `name: phase4-otel` → `name: phase4-schedext`.
2. `branches: [feat/phase4.3-otel]` → `branches: [feat/phase12.2a-schedext]`.
3. **Eliminar el step de build del otelcol vía ocb** (el `go run go.opentelemetry.io/collector/cmd/builder` que instala `dist/otelcol`) — schedext no usa el collector; el scheduler viene del paquete `scx` horneado.
4. El step final del gate: `bash tests/boot/otel-export-and-assert.sh "<image-arg>"` → `sudo bash tests/boot/schedext-and-assert.sh "<image-arg>"` (mismo `<image-arg>` que usaba otel, p.ej. `image/v1.raw`; mantener el `sudo` si el step original lo tenía). Si el step tiene `name:`, reescribirlo a `"Gate: sched_ext custom scheduler active"` (entre comillas por el `:`).
5. Reescribir el comentario NOTE del header (heredado de otel) por: §12.2a es OS-puro (sin cambio de runtime); el gate prueba que el OS carga un scheduler sched_ext custom (suite `scx`) y astromeshd corre bajo él; necesita KVM + kernel con CONFIG_SCHED_CLASS_EXT (Debian cloud 6.12+, ADR-3).

Verificar: `grep -nE 'name: phase4-schedext|feat/phase12.2a-schedext|schedext-and-assert.sh' .github/workflows/phase4-schedext.yml` (tres matches) y `grep -n 'otel-export-and-assert.sh\|cmd/builder\|otelcol\|feat/phase4.3-otel' .github/workflows/phase4-schedext.yml` (sin matches). Si pyyaml: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/phase4-schedext.yml'))" && echo YAML_OK`.

- [ ] **Step 4: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add tests/local/dev-loop.sh .github/workflows/phase4-schedext.yml
git commit -m "test(12.2a): dev-loop schedext target + CI workflow (1-VM custom-scheduler gate)"
```

- [ ] **Step 5: Correr el gate local (la prueba — REQUIERE WSL2+KVM; no corre en macOS)**

Run (host Windows):
```bash
wsl -d Debian -u root -- bash /mnt/d/monaccode/astromesh-os/tests/local/dev-loop.sh schedext 2>&1 | tail -n 50
```
Expected: `SCHEDEXT GATE PASSED`. **Iteración esperada (los dos riesgos R-mayores del spec §7):**
> 1. **Kernel:** si `SCHEDEXT-SUPPORTED` falla (`/sys/kernel/sched_ext` ausente), el kernel Debian no trae `CONFIG_SCHED_CLASS_EXT` → descubrimiento mayor (pinear/configurar kernel con sched_ext). Documentar.
> 2. **Paquete scx:** si `mkosi build` falla en el apt de `scx`, o `SCHEDEXT-ACTIVE` falla porque no hay binario / el load falla → resolver el nombre del paquete o caer al fallback build-input (Tarea 3 Step 4). Documentar el binario que funcionó.

---

## Task 6: Roadmap

**Files:**
- Modify: `docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`

- [ ] **Step 1: Localizar la tabla de roadmap**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && grep -n "§12\|12.2\|sysext-gpu\|^| " docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`
La tabla tiene 4 columnas (5 pipes). Usar la fila `4.5`/`sysext-gpu` como molde de estructura.

- [ ] **Step 2: Agregar las filas §12.2a (done) y §12.2b (diferido)**

Agregar al final de la tabla, como líneas únicas con el mismo número de `|` que las vecinas:

```
| §12.2a | **sched_ext** — cargar un scheduler de CPU custom (suite scx), gated (post-4, F) | scheduler custom activo + astromeshd funcional bajo él | ✅ **HECHO** (2026-06-11) — gate `SCHEDEXT GATE PASSED` (SUPPORTED + state==enabled + /v1/health 200). Política agent-aware → futuro. |
| §12.2b | GPU broker (arbitraje de GPU entre agentes) | — | diferido (no testeable sin GPU; va con sysext-gpu) |
```

(Contar los `|` de una fila vecina y replicar. Una línea por fila.)

- [ ] **Step 3: Verificar integridad**

Run: `cd /Users/fulfaro/monaccode/astromesh-os && grep -n "SCHEDEXT GATE PASSED\|12.2a\|12.2b" docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md`
Expected: ambas filas presentes; contar `|` == vecinas.

- [ ] **Step 4: Commit**

```bash
cd /Users/fulfaro/monaccode/astromesh-os
git add docs/superpowers/specs/2026-06-09-phase4.1-machineconfig-design.md
git commit -m "docs(12.2a): roadmap — sched_ext done; GPU broker (12.2b) deferred"
```

---

## Self-Review

**Spec coverage:**
- §1 loader gated + self-check (SUPPORTED/ACTIVE/FUNCTIONAL) → Task 2 (4 archivos).
- §1 gated por `spec.sched_ext.enabled` → Task 1 (machine-config) + guards en Task 2.
- §2 binario del scheduler → Task 3 (paquete `scx`; fallback build-input documentado en Step 4 — refina §2).
- §3 gate (smbios sched_ext:true, /v1/health, grep markers) → Task 4.
- §4 componentes (loader, check, machine-config, mkosi/postinst, gate, dev-loop, CI) → Tasks 1–5.
- §6 done (local verde, CI, gates previos verdes, roadmap 12.2a+12.2b) → Task 5 Step 5 + Task 6.
- §7 riesgos (kernel CONFIG_SCHED_CLASS_EXT; paquete scx/build) → Task 5 Step 5 (iteración) + Task 3 Step 4 (fallback).

**Placeholder scan:** Sin TBD/TODO. La iteración (Task 5 Step 5) y el fallback (Task 3 Step 4) son ítems empíricos deliberados de WSL con comandos/criterios concretos, no placeholders. El loader auto-detecta el binario (no asume uno fijo).

**Type/contract consistency:** Flag `spec.sched_ext.enabled` escrito por la machine-config (Task 1) ↔ leído por el guard del loader y del check (Task 2) ↔ credencial `sched_ext: true` del gate (Task 4). Marcadores `SCHEDEXT-SUPPORTED OK`/`SCHEDEXT-ACTIVE OK`/`RUNTIME-FUNCTIONAL OK`/`SCHEDEXT GATE OK`/`SCHEDEXT FAILED` (in-VM, Task 2) ↔ el gate grepea `SCHEDEXT GATE OK` (pass) y `SCHEDEXT FAILED` (bail) (Task 4); el host imprime `SCHEDEXT GATE PASSED`. Rutas `/usr/lib/astromesh-os/schedext/astromesh-schedext.sh` y `…-check.sh` idénticas en las unidades (Task 2 ExecStart) y el postinst (Task 3). Unidades `astromesh-schedext.service`/`astromesh-schedext-check.service` consistentes (Task 2 + postinst enable). El loader corre el binario detectado; el check lee `/sys/kernel/sched_ext/state`+`root/ops` (no asume el nombre del scheduler). `/v1/health` puerto 8000 consistente. Consistente.
